---
phase: 15-provider-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified: []
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "A Google OAuth sign-in creates a valid row in public.players with no errors"
    - "A Discord OAuth sign-in creates a valid row in public.players with no errors"
    - "An email/password user who signs in via OAuth with the same email has exactly one row in auth.users"
    - "The Discord raw_user_meta_data shape is confirmed and logged — user_name field presence verified"
  artifacts:
    - path: "auth.users (Supabase)"
      provides: "Google and Discord test OAuth user rows"
    - path: "public.players (Supabase)"
      provides: "Corresponding player rows for OAuth test users with non-NULL name"
  key_links:
    - from: "OAuth sign-in flow"
      to: "handle_new_user trigger"
      via: "auth.users INSERT fires trigger → inserts to public.players"
      pattern: "manual"
    - from: "Discord raw_user_meta_data"
      to: "handle_new_user trigger display name COALESCE"
      via: "user_name field read from JSONB"
      pattern: "manual"
---

<objective>
Perform end-to-end OAuth flow verification for both Google and Discord, confirm the trigger creates valid player records, verify identity linking, and log the actual Discord metadata shape.

Purpose: Infrastructure is only confirmed working when a real OAuth flow completes and the database shows the correct results. The Discord metadata shape must also be confirmed against the live Supabase response (MEDIUM confidence item from research) before Phase 16 builds against it.

Output: All four SETUP requirements confirmed as met; Discord metadata shape documented in SUMMARY.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-provider-infrastructure/15-RESEARCH.md
@.planning/phases/15-provider-infrastructure/15-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Perform Google and Discord OAuth flows and verify database results</name>
  <files>none — live OAuth flow testing against Supabase</files>
  <action>
    Human-required verification step. Initiate Google and Discord OAuth flows from the app and inspect the resulting database rows via Supabase SQL Editor.
    See how-to-verify block below for full instructions.
  </action>
  <verify>
    Manual: public.players shows non-NULL name for both Google and Discord test users; auth.users COUNT = 1 for shared-email merge test; Discord raw_user_meta_data JSON shape logged.
  </verify>
  <done>
    Google OAuth flow completes with valid player record, Discord OAuth flow completes with valid player record, identity linking confirmed, Discord metadata shape captured and included in SUMMARY.
  </done>
  <what-built>
    SQL trigger migration applied (Plan 01), both providers registered and enabled in Supabase (Plan 02). Ready to test live OAuth flows.
  </what-built>
  <how-to-verify>
    Perform the following flows and SQL checks. Test in a dev or staging environment — use test accounts, not production accounts.

    ---

    **A. Google OAuth flow (SETUP-01 + SETUP-04)**

    1. Start the app locally (`npm run dev`) or use your staging URL
    2. Navigate to `/auth/login` (or any route that will eventually have OAuth buttons — for now you can call `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin + '/auth/callback' } })` from browser console)
    3. Complete the Google OAuth consent screen with a test Google account
    4. Confirm: app redirects back and user is authenticated (no error URL)
    5. In Supabase SQL Editor, run:
       ```sql
       SELECT id, email, raw_user_meta_data FROM auth.users ORDER BY created_at DESC LIMIT 3;
       SELECT id, name, email, avatar_url FROM public.players ORDER BY created_at DESC LIMIT 3;
       ```
    6. Confirm: the Google test user appears in both tables, `public.players.name` is NOT NULL and shows the Google account name (not empty)

    ---

    **B. Discord OAuth flow (SETUP-02 + SETUP-04) + metadata inspection**

    1. Repeat the OAuth flow with Discord instead of Google
    2. In Supabase SQL Editor, run:
       ```sql
       SELECT id, email, raw_user_meta_data FROM auth.users WHERE email = '<discord-test-email>' ORDER BY created_at DESC LIMIT 1;
       SELECT id, name, email, avatar_url FROM public.players WHERE email = '<discord-test-email>' ORDER BY created_at DESC LIMIT 1;
       ```
    3. Confirm: `public.players.name` is NOT NULL and shows the Discord username
    4. IMPORTANT — log the Discord metadata shape: Copy the full `raw_user_meta_data` JSON for the Discord user. Note which keys are present (especially: is the display name stored under `user_name`, `username`, or `full_name`?). Include this in your resume response below — it is needed for Phase 16 trigger and sync code.

    ---

    **C. Identity linking verification (SETUP-03)**

    1. Create a fresh email/password account in the app with an email address you also control via Google or Discord
    2. Verify the email address (click the confirmation link)
    3. Sign out
    4. Sign in via Google or Discord using that same email address
    5. In Supabase SQL Editor, run:
       ```sql
       SELECT COUNT(*) FROM auth.users WHERE email = '<your-test-email>';
       ```
    6. Confirm: COUNT is 1 (not 2 — automatic linking merged to one record)

  </how-to-verify>
  <resume-signal>
    Type "verified" followed by the Discord raw_user_meta_data JSON shape. Example: "verified — Discord metadata: {"user_name":"testuser","avatar_url":"https://cdn.discordapp.com/..."}"
    If any step fails, describe what went wrong.
  </resume-signal>
</task>

</tasks>

<verification>
- Google OAuth flow completes without error; player row in public.players has non-NULL name
- Discord OAuth flow completes without error; player row in public.players has non-NULL name
- Same-email identity merge confirmed: COUNT(*) = 1 in auth.users for shared email
- Discord raw_user_meta_data shape logged and included in SUMMARY for Phase 16 use
</verification>

<success_criteria>
All four SETUP requirements are confirmed met by live OAuth flows:
- SETUP-01: Google flow creates valid player record
- SETUP-02: Discord flow creates valid player record
- SETUP-03: Same-email OAuth merges to single auth.users record
- SETUP-04: Trigger handles provider metadata with no NULL errors in either flow
Phase 15 is complete. Phase 16 (OAuth UI and Profile Sync) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/15-provider-infrastructure/15-03-SUMMARY.md`

IMPORTANT: The SUMMARY must include the confirmed Discord raw_user_meta_data field names — this is a hard dependency for Phase 16 trigger and sync code. Record the actual JSON shape observed during testing.
</output>

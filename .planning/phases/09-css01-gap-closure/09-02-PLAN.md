---
phase: 09-css01-gap-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified: []
autonomous: false
requirements:
  - CSS-01
gap_closure: true

must_haves:
  truths:
    - "Home page renders correctly — hero, two-column layout, gamerooms grid, leaderboard card, stats cards all visible"
    - "Leaderboard page renders with correct .container/.main layout (styles added in Phase 7 were previously no-ops)"
    - "Admin lobby page renders correctly after comment-only CSS diffs in Phase 7"
    - "Answer reveal animation fires progressively — cards transition to visible one-by-one on correct answer submit"
    - "Performance mode suppresses all DOM effects — no colorBurstOverlay, screenShake, or successGlow when performance mode is on"
    - "WebSocket listener count does not accumulate — stays constant across navigate-away/navigate-back cycles"
  artifacts:
    - path: ".planning/phases/09-css01-gap-closure/09-02-SUMMARY.md"
      provides: "Human-approved visual regression sign-off for Phase 7 and Phase 8 work"
  key_links:
    - from: "human visual check"
      to: "Phase 7 CSS reorganization"
      via: "browser rendering at http://localhost:3000 and admin routes"
      pattern: "n/a"
    - from: "human visual check"
      to: "Phase 8 bug fixes and performance"
      via: "game interaction testing in live dev server"
      pattern: "n/a"
---

<objective>
Human browser visual regression checkpoint covering all pending checks from Phase 7 (Admin/Route CSS
Tidy) and Phase 8 (Bug Fixes and Performance).

Purpose: Phase 7 and Phase 8 both deferred their human-verify checkpoints. This plan collects all 6
pending visual checks into a single gate before the milestone is closed. No code changes occur in
this plan — it is a pure verification checkpoint.

Output: Human approval confirming all 6 browser checks pass. Failure descriptions (if any) must be
captured in the summary so gap closure plans can be created.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-css01-gap-closure/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm build</name>
  <files></files>
  <action>
    Run npm run build to confirm the Next.js build is clean after the Phase 09-01 code fixes.
    If the build succeeds, start the dev server (npm run dev) and confirm it is listening on
    http://localhost:3000.

    Output the build result and the URL to the user so they can begin visual checks.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
    Expected: build exits 0 with no errors.
  </verify>
  <done>Dev server is running and build is clean. User is ready to begin browser checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual regression — Phase 7 checks (3 items)</name>
  <files></files>
  <action>
    Pause execution and present the human verification instructions below. Wait for human approval
    before proceeding to Task 3.
  </action>
  <what-built>
    Phase 7 (Admin/Route CSS Tidy) reorganized page.module.css into per-consumer modules and added
    .container/.main to leaderboard.module.css (previously undefined no-ops that now have real CSS).
    Admin route CSS files received comment-only diffs (section headers added, no property changes).
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in a browser. Check each item:

    CHECK 1 — Home page layout (http://localhost:3000)
    Expected: Hero section visible, two-column layout intact, gamerooms grid renders, leaderboard
    card visible, stats cards visible. Resize below 900px — layout should stack to single column.
    Pass: Everything renders and is visually correct. No broken layout, missing sections, or
    unexpected spacing.

    CHECK 2 — Leaderboard page layout (http://localhost:3000/leaderboard)
    Expected: The page renders with its .container and .main CSS classes now applied (.container
    and .main were previously undefined no-ops; Phase 7 added real styles to leaderboard.module.css).
    Verify the leaderboard list is properly contained and laid out.
    Pass: Page renders correctly with no layout regression from the new .container/.main styles.

    CHECK 3 — Admin lobby page (http://localhost:3000/admin/lobbies/[any-lobby-id])
    Expected: Admin lobby page renders correctly. Phase 7 diffs to admin CSS files were comment-only
    (section navigation headers added) — should be a no-op visually.
    Pass: Admin lobby page looks identical to before. No layout issues, no missing styles.
  </how-to-verify>
  <verify>Human types "phase7-approved" or describes failures.</verify>
  <done>Human has confirmed all 3 Phase 7 visual checks pass (or documented failures).</done>
  <resume-signal>
    Type "phase7-approved" if all 3 Phase 7 checks pass, or describe any failures (which check
    failed and what was wrong visually).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual regression — Phase 8 checks (3 items)</name>
  <files></files>
  <action>
    Pause execution and present the human verification instructions below. Wait for human approval
    before creating the summary.
  </action>
  <what-built>
    Phase 8 (Bug Fixes and Performance) fixed the answer reveal animation type mismatch (BUG-02),
    gated all DOM effects on performanceModeAtom (PERF-01), and fixed WebSocket listener accumulation
    in useGameEvents.ts (REL-01).
  </what-built>
  <how-to-verify>
    Join an active game room (or create a test game). Check each item:

    CHECK 4 — Answer reveal animation (requires active game round)
    Steps: Submit a correct answer during a round. After the round ends, watch the answer reveal.
    Expected: Answer cards transition to visible one-by-one progressively (the styles.visible class
    is applied per-card with staggered timing). Cards should not all appear at once or remain hidden.
    Pass: At least one card visibly animates in during reveal.

    CHECK 5 — Performance mode DOM suppression
    Steps: Enable performance mode (find the performance mode toggle in the UI). Submit a correct
    answer.
    Expected: NO colorBurstOverlay, NO screenShake effect, NO successGlow DOM element injected.
    With DevTools Elements panel open, confirm none of these class names appear in the DOM after
    correct answer submission.
    Pass: DevTools shows no overlay/shake/glow elements. Only normal game state updates occur.

    CHECK 6 — WebSocket listener accumulation stress test
    Steps:
    1. Open DevTools (Network or a console listener counter if available).
    2. Join a game room and stay for 10-15 seconds.
    3. Navigate away from the game room (back to home).
    4. Navigate back into the same game room.
    5. Repeat navigate-away/navigate-back 3-4 times.
    Expected: WebSocket event listener count stays constant. No error messages like "listener
    registered N times". If you have access to the browser console, confirm no accumulation warnings.
    Pass: No listener accumulation observed across multiple navigation cycles.
  </how-to-verify>
  <verify>Human types "phase8-approved" or describes failures.</verify>
  <done>Human has confirmed all 3 Phase 8 visual checks pass (or documented failures).</done>
  <resume-signal>
    Type "phase8-approved" if all 3 Phase 8 checks pass, or describe any failures (which check
    number failed and what was observed vs expected).
  </resume-signal>
</task>

</tasks>

<verification>
All 6 browser checks approved by human:
- Phase 7: Home page layout, leaderboard page layout, admin lobby page
- Phase 8: Answer reveal animation, performance mode suppression, listener accumulation test

If any check fails, capture the failure in the summary and create a follow-up gap closure plan.
</verification>

<success_criteria>
- Human approves all 6 visual checks (3 Phase 7 + 3 Phase 8)
- No visual regressions found in any checked page or interaction
- Milestone v1.2 CSS-01 gap is confirmed closed
- All 8 v1.2 requirements (CSS-01, CSS-02, CSS-03, BUG-01, BUG-02, PERF-01, PERF-02, REL-01) are
  now SATISFIED
</success_criteria>

<output>
After completion, create `.planning/phases/09-css01-gap-closure/09-02-SUMMARY.md`
</output>

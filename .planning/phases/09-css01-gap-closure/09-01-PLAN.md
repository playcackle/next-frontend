---
phase: 09-css01-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/SlotGrid.module.css
  - src/app/gameroom/components/SlotGrid.tsx
  - src/app/gameroom/components/UnifiedMessages.tsx
autonomous: true
requirements:
  - CSS-01
gap_closure: true

must_haves:
  truths:
    - "SlotGrid renders with visible layout toggle styling and grid layout"
    - "SlotGrid.tsx has no reference to SlotTile (no import, no JSX usage)"
    - "UnifiedMessages.tsx has no debugger; statement"
    - "TypeScript compilation produces no new errors from SlotGrid.tsx"
  artifacts:
    - path: "src/app/gameroom/components/SlotGrid.module.css"
      provides: "Full layout toggle and grid styles — 91 lines restored from commit 6a5e42c"
      contains: ".slotGrid, .layoutToggleRow, .layoutToggleBtn, .layoutToggleActive, .layoutToggleTrack, .layoutToggleThumb, .layoutToggleThumbOn"
    - path: "src/app/gameroom/components/SlotGrid.tsx"
      provides: "GRID-only SlotGrid — layout toggle present but TILES branch removed"
      contains: "AnswerGrid"
    - path: "src/app/gameroom/components/UnifiedMessages.tsx"
      provides: "Production-safe message classifier with no debugger statement"
  key_links:
    - from: "src/app/gameroom/components/SlotGrid.tsx"
      to: "src/app/gameroom/components/SlotGrid.module.css"
      via: "import styles from './SlotGrid.module.css'"
      pattern: "styles\\.slotGrid|styles\\.layoutToggle"
    - from: "src/app/gameroom/components/SlotGrid.tsx"
      to: "src/app/gameroom/components/AnswerGrid.tsx"
      via: "<AnswerGrid slots={slots} />"
      pattern: "AnswerGrid"
---

<objective>
Restore SlotGrid.module.css from git history, remove the TILES layout branch from SlotGrid.tsx
(SlotTile was deleted in commit d6c3fdb and will not be restored), and remove the bare debugger;
statement from UnifiedMessages.tsx line 46.

Purpose: Close the CSS-01 gap left by the manual d6c3fdb refactor commit. SlotGrid currently
renders with 6 undefined CSS classes (no styles) and references a deleted component. UnifiedMessages
contains a production-unsafe debugger statement.

Output:
- SlotGrid.module.css — 91-line file restored verbatim from git commit 6a5e42c
- SlotGrid.tsx — TILES branch removed; only GRID (AnswerGrid) path remains; layout toggle kept
  but TILES button removed (toggle becomes a no-op or is simplified to remove dead path)
- UnifiedMessages.tsx — debugger; removed from line 46
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore SlotGrid.module.css from git history</name>
  <files>src/app/gameroom/components/SlotGrid.module.css</files>
  <action>
    The file is currently 0 bytes — emptied by commit d6c3fdb. Restore it verbatim from commit 6a5e42c
    using git show. Run:

      git show 6a5e42c:src/app/gameroom/components/SlotGrid.module.css > src/app/gameroom/components/SlotGrid.module.css

    Verify the file is non-empty after restoration. The restored content is 91 lines and defines these
    CSS module classes:
      - .slotGrid (3-column grid, gap 1.5rem)
      - .layoutToggleRow, .layoutToggleLabel
      - .layoutToggleBtn, .layoutToggleActive
      - .layoutToggleTrack, .layoutToggleThumb, .layoutToggleThumbOn
      - @media (max-width: 1024px) and @media (max-width: 768px) overrides for .slotGrid

    Do NOT modify the restored content. This is a verbatim restoration.
  </action>
  <verify>
    <automated>wc -l src/app/gameroom/components/SlotGrid.module.css</automated>
    Expected: 91 lines. Also run: grep -c "\.slotGrid\|layoutToggle" src/app/gameroom/components/SlotGrid.module.css
    Expected: 7 or more matches.
  </verify>
  <done>SlotGrid.module.css is non-empty, contains .slotGrid and all layoutToggle* classes, matches commit 6a5e42c content exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove TILES layout branch from SlotGrid.tsx</name>
  <files>src/app/gameroom/components/SlotGrid.tsx</files>
  <action>
    The current SlotGrid.tsx (src/app/gameroom/components/SlotGrid.tsx) references SlotTile which
    was deleted in commit d6c3fdb. The decision is to NOT restore SlotTile — instead remove the
    TILES layout branch entirely.

    Current state of the file:
    - Line 8: import styles from "./SlotGrid.module.css" (keep)
    - No import SlotTile line is present but lines 48-57 reference &lt;SlotTile&gt; in JSX
    - Line 12: const [useGridLayout, setUseGridLayout] = useState(true); (remove — toggle no longer needed)
    - Lines 16-43: layout toggle UI with TILES/GRID buttons and track (remove the entire toggle row,
      OR simplify — see below)
    - Lines 44-58: conditional render: useGridLayout ? &lt;AnswerGrid /&gt; : &lt;SlotTile loop&gt; (remove TILES branch)

    Decision: Remove the layout toggle entirely since TILES mode no longer exists. Simplify SlotGrid
    to render only &lt;AnswerGrid slots={slots} /&gt;.

    After edit the component should look like:

    ```tsx
    "use client";

    import { useAtomValue } from "jotai";
    import React from "react";
    import { slotsAtom } from "../store/gameAtoms";
    import AnswerGrid from "./AnswerGrid";

    function SlotGrid() {
      const slots = useAtomValue(slotsAtom);

      return &lt;AnswerGrid slots={slots} /&gt;;
    }

    export default React.memo(SlotGrid);
    ```

    Remove:
    - The useState import (no longer needed after toggle removal)
    - The Slot type import from "../types/state" (no longer needed without the .map)
    - The styles import from "./SlotGrid.module.css" (no longer needed without toggle UI)
    - The [useGridLayout, setUseGridLayout] state
    - The entire layoutToggleRow div (lines 16-43)
    - The conditional render — keep only &lt;AnswerGrid slots={slots} /&gt;
    - The slotGrid div and SlotTile map (lines 47-57)

    NOTE: The SlotGrid.module.css is being restored in Task 1 (for completeness/history). After this
    simplification, SlotGrid.tsx will no longer import it. That is acceptable — the CSS file will
    exist but be unused until a future feature restores the toggle. Do NOT delete the CSS file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep SlotGrid</automated>
    Expected: no output (no TypeScript errors for SlotGrid). Also run:
    grep -n "SlotTile\|useGridLayout\|layoutToggle\|debugger" src/app/gameroom/components/SlotGrid.tsx
    Expected: no output (none of these strings present).
  </verify>
  <done>SlotGrid.tsx renders only AnswerGrid, has no SlotTile reference, no useState, no layout toggle UI, TypeScript reports no errors for this file.</done>
</task>

<task type="auto">
  <name>Task 3: Remove debugger; from UnifiedMessages.tsx</name>
  <files>src/app/gameroom/components/UnifiedMessages.tsx</files>
  <action>
    Remove line 46 from src/app/gameroom/components/UnifiedMessages.tsx.

    Current line 46 contains exactly:
      debugger;

    It sits inside the getMessageTypeClass function, immediately after the botBobMessage early-return
    block (lines 40-45) and before the switch statement (line 47).

    Remove only that single line. Do not change any surrounding code. The function should flow
    directly from the closing brace of the if-block on line 45 into the switch statement.

    After removal the function body should read:
      if (msg.player_id === "botbob" || msg.display_name.toLowerCase() === "botbob") {
        return styles.botBobMessage;
      }
      switch (msg.message_type) { ...
  </action>
  <verify>
    <automated>grep -n "debugger" src/app/gameroom/components/UnifiedMessages.tsx</automated>
    Expected: no output (no debugger statement present).
  </verify>
  <done>UnifiedMessages.tsx contains no debugger statement. The getMessageTypeClass function is otherwise unchanged.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. SlotGrid.module.css is 91 lines and contains all layoutToggle* and .slotGrid classes:
   wc -l src/app/gameroom/components/SlotGrid.module.css  # → 91

2. SlotGrid.tsx has no SlotTile, no useGridLayout, no layoutToggle references:
   grep -n "SlotTile\|useGridLayout\|layoutToggle" src/app/gameroom/components/SlotGrid.tsx  # → no output

3. TypeScript build is clean for the modified files:
   npx tsc --noEmit 2>&1 | grep -E "SlotGrid|UnifiedMessages"  # → no output

4. UnifiedMessages.tsx has no debugger:
   grep -n "debugger" src/app/gameroom/components/UnifiedMessages.tsx  # → no output

5. Next.js build completes without errors:
   npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- SlotGrid.module.css contains the full 91-line layout toggle and grid styles (verbatim from commit 6a5e42c)
- SlotGrid.tsx imports and renders only AnswerGrid — no SlotTile reference anywhere in the file
- UnifiedMessages.tsx has no debugger; statement
- TypeScript reports no new errors (npx tsc --noEmit clean for these files)
- Next.js build succeeds (npm run build exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/09-css01-gap-closure/09-01-SUMMARY.md`
</output>

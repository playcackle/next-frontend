---
phase: 06-gameroom-css-split
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/UnifiedMessages.module.css
  - src/app/gameroom/components/UnifiedMessages.tsx
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "Messages scroll area renders with purple scrollbar and correct padding"
    - "Each message type (chat, answer, successful, botBob, duplicate, ownMessage) renders with correct border-left color and background tint"
    - "Successful answer message glows on appear"
    - "Performance mode (:global(.performance-mode)) suppresses glow and box-shadow on botBobMessage"
  artifacts:
    - path: src/app/gameroom/components/UnifiedMessages.module.css
      provides: "Messages container, scroll area, message type variants, content wrapper, performance mode overrides"
      contains: "unifiedMessagesContainer"
  key_links:
    - from: src/app/gameroom/components/UnifiedMessages.tsx
      to: src/app/gameroom/components/UnifiedMessages.module.css
      via: "import styles from './UnifiedMessages.module.css'"
      pattern: "UnifiedMessages\\.module\\.css"
---

<objective>
Extract all UnifiedMessages styles from `gameroom.module.css` into `UnifiedMessages.module.css`. This covers the messages container, scrollable area with custom scrollbar, all message type variants, the content wrapper, animations, and the two :global(.performance-mode) overrides.

Purpose: UnifiedMessages is the richest message-display component — ~350 lines of its own styles.
Output: UnifiedMessages.module.css, updated UnifiedMessages.tsx.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UnifiedMessages.module.css and update import</name>
  <files>src/app/gameroom/components/UnifiedMessages.module.css, src/app/gameroom/components/UnifiedMessages.tsx</files>
  <action>
Create `src/app/gameroom/components/UnifiedMessages.module.css` with the following classes extracted verbatim from `gameroom.module.css`:

**Messages container (lines 715–728):**
- `.unifiedMessagesContainer` — dark bg, border-radius 8px, purple box-shadow, backdrop-filter, flex column, transition, margin-left 4px, max-height 60vh

**Scroll area (lines 1271–1315):**
- `.messagesScrollArea` — flex:1, padding, overflow-y auto, flex column, gap, scroll-behavior smooth, purple scrollbar
- `.messagesScrollArea::-webkit-scrollbar` — width 8px
- `.messagesScrollArea::-webkit-scrollbar-track` — dark bg, border-radius, neon-purple border
- `.messagesScrollArea::-webkit-scrollbar-thumb` — purple-to-cyan gradient, border-radius, glow
- `.messagesScrollArea::-webkit-scrollbar-thumb:hover` — brighter gradient, enhanced glow
- `.messagesEmpty` — muted white italic centered, retro-font-alt

**Base message (lines 1325–1333):**
- `.unifiedMessage` — padding 0.25rem, padding-left 0.5rem, border-radius, font-size 0.7rem, messageAppear animation, retro-font-alt, transparent border-left, dark bg

**Message type variants (lines 1336–1388):**
- `.chatMessage` — neon-purple border-left, purple bg tint
- `.answerMessage` — neon-green border-left, green bg tint, retro-font
- `.successfulAnswerMessage` — neon-green border-left, green bg tint, correctAnswerGlow animation
- `.ownMessage` (first instance, lines ~873–876) — neon-blue border-left, blue bg tint (note: there are two `.ownMessage` rules in gameroom.module.css; include both, the second one at lines 1385–1388 uses !important)
- `.botBobMessage` — neon-blue border-left, cyan bg tint (lines 1643–1647)
- `.duplicateMessage` — #ff8c00 border-left, orange bg tint, opacity 0.75 (lines 1649–1654)
- `.ownSuccessfulAnswerMessage` — neon-green !important override (lines 1656–1661)

**Message content structure (lines 1354–1377, 1620–1637):**
- `.messagePrefix` — bold 0.8rem uppercase letter-spacing
- `.messageUser` — neon-blue 0.8rem
- `.messageTime` — 0.7rem muted white, margin-left auto
- `.messageContent` — margin-bottom 0.25rem, line-height 1, font-size 1.1rem (first instance)
- `.canonicalAnswer` — neon-green italic opacity 0.8
- `.messageContentWrapper` — flex:1, column, gap 0.25rem, min-width 0, padding-left 0.25rem
- `.unifiedMessage` second rule (lines 1630–1632) — padding 0.1rem override
- `.messageContent` second rule (lines 1634–1637) — word-wrap/overflow-wrap break-word

**Animations (lines 862–871, 1484–1494):**
- `@keyframes messageAppear` — opacity 0→1, translateY 10px→0
- `@keyframes correctAnswerGlow` — box-shadow neon-green pulse 3-stop

**Performance mode overrides (lines 1663–1670):**
- `:global(.performance-mode) .successfulAnswerMessage` — animation: none
- `:global(.performance-mode) .botBobMessage` — box-shadow: none

In `UnifiedMessages.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./UnifiedMessages.module.css"`.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "UnifiedMessages" || echo "no UnifiedMessages errors"</automated>
  </verify>
  <done>UnifiedMessages.module.css exists with container, scroll area, all message type classes, content wrapper, animations, and performance mode overrides. UnifiedMessages.tsx imports from ./UnifiedMessages.module.css.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — no new errors. Confirm `src/app/gameroom/components/UnifiedMessages.module.css` exists and contains `.unifiedMessagesContainer`, `.messagesScrollArea`, `.successfulAnswerMessage`, `:global(.performance-mode)`.
</verification>

<success_criteria>
- UnifiedMessages.module.css created with all message display styles and performance mode overrides
- UnifiedMessages.tsx import updated
- TypeScript passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-05-SUMMARY.md`
</output>

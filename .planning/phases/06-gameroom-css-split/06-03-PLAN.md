---
phase: 06-gameroom-css-split
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/SlotTile.module.css
  - src/app/gameroom/components/SlotTile.tsx
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "Unanswered slots show question mark / points value in correct size and retro font"
    - "Answered slots show correct answer text and player badge with shimmer entrance animation"
    - "Bonus tile pulse animation plays in purple, normal tile pulse in green"
    - "correctPulse and screenShake animations fire on newly snapped slots"
  artifacts:
    - path: src/app/gameroom/components/SlotTile.module.css
      provides: "Slot tile appearance, answered content, player badge, pulse/shake animations"
      contains: "slotTile"
  key_links:
    - from: src/app/gameroom/components/SlotTile.tsx
      to: src/app/gameroom/components/SlotTile.module.css
      via: "import styles from './SlotTile.module.css'"
      pattern: "SlotTile\\.module\\.css"
---

<objective>
Extract the SlotTile styles from `gameroom.module.css` into `SlotTile.module.css`. This is the largest single-component extraction — includes slot tile base styles, answered-state styles, player badge with dramatic entrance animation, shimmer, pulse animations, and screen shake keyframes.

Purpose: The heaviest block of component-specific styles — once extracted, gameroom.module.css loses ~250 lines.
Output: SlotTile.module.css, updated SlotTile.tsx.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SlotTile.module.css with tile, badge, and animation styles</name>
  <files>src/app/gameroom/components/SlotTile.module.css, src/app/gameroom/components/SlotTile.tsx</files>
  <action>
Create `src/app/gameroom/components/SlotTile.module.css` with the following classes extracted verbatim from `gameroom.module.css`.

**Slot tile base (lines 665–699):**
- `.slotTile` — neon-pink bg, border-radius 8px, flex column centered, transition, relative, overflow hidden
- `.bonusTile` — neon-purple bg override
- `.answered` — opacity 1, green border and glow, cursor default

**Slot contents (lines 498–528):**
- `.answeredContent` — flex column centered, full width/height, dark bg
- `.correctAnswer` — 0.85rem bold white, text-shadow, line-clamp 2, min-height 2.5rem

**Question mark (lines 702–709):**
- `.questionMark` — 3.5rem bold retro-font, white text-shadow

**Player badge (lines 562–663):**
- `.playerBadge` — dark gradient bg, border-radius 2px, padding, relative, width 100%, animation badgeAppear 1.2s
- `.playerBadge::before` — shimmer sweep gradient (50%/200% size)
- `.playerBadge::after` — inset glow box-shadow pseudo
- `.playerBadgeAvatar` — 20×20px, border-radius 3px, avatarPulse animation
- `.playerBadgeName` — 0.75rem white, nowrap, text-overflow ellipsis
- `@keyframes badgeAppear` — 6-stop bounce entrance with filter brightness/blur
- `@keyframes badgeShimmer` — translateX/Y shimmer sweep
- `@keyframes avatarPulse` — box-shadow pulse alternate

**Pulse animations (lines 761–843):**
- `@keyframes pulse` — green scale+brightness+box-shadow 5-stop
- `@keyframes pulsePurple` — purple scale+brightness+box-shadow 5-stop
- `.correctPulse` — applies pulse + screenShake animations
- `.bonusTile.correctPulse` — applies pulsePurple + screenShake

**Screen shake (lines 940–974):**
- `@keyframes screenShake` — 11-stop translate+rotate shake

**Success glow (lines 1064–1103):**
- `.successGlow` — absolute pseudo-element radial-gradient green, successGlowPulse animation
- `.successGlow.bonus` — purple variant
- `@keyframes successGlowPulse` — opacity+scale 4-stop

**Slot highlight and bonus glow (lines 1231–1269):**
- `.slotTile.otherPlayerAnswering` — scale+box-shadow animation
- `@keyframes otherPlayerAnswering` — 3-stop scale
- `.bonusTile.answered` — bonusGlow animation infinite
- `@keyframes bonusGlow` — box-shadow neon-purple pulse alternate

In `SlotTile.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./SlotTile.module.css"`.

Do NOT include `colorBurstOverlay`, `particleContainer`, `particle`, `particlePurple`, `fullScreenShake`, `shake`, `colorFlash`, `zoomEffect`, `rotateEffect` — these are applied by `page.tsx` to the main wrapper, not by SlotTile.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "SlotTile" || echo "no SlotTile errors"</automated>
  </verify>
  <done>SlotTile.module.css exists containing all tile, badge, and animation classes. SlotTile.tsx imports from ./SlotTile.module.css. TypeScript passes for SlotTile.tsx.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — no new errors. Confirm `src/app/gameroom/components/SlotTile.module.css` exists and contains `.slotTile`, `.playerBadge`, `@keyframes badgeAppear`, `@keyframes screenShake`.
</verification>

<success_criteria>
- SlotTile.module.css created with all tile, badge, animation styles (~250 lines)
- SlotTile.tsx import updated
- TypeScript passes
- Page-level animation classes (colorBurstOverlay, particle, shake, colorFlash etc.) remain in gameroom.module.css
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-03-SUMMARY.md`
</output>

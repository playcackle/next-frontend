---
phase: 06-gameroom-css-split
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/SlotGrid.module.css
  - src/app/gameroom/components/SlotGrid.tsx
  - src/app/gameroom/components/UnifiedInputForm.module.css
  - src/app/gameroom/components/UnifiedInputForm.tsx
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "SlotGrid layout toggle renders with TILES/GRID buttons, track, and thumb in correct colors"
    - "Active layout button highlights in neon-pink"
    - "Unified input renders with the conic-gradient animated border on focus"
    - "Answer mode input uses purple-to-blue gradient, chat mode uses purple-to-cyan"
  artifacts:
    - path: src/app/gameroom/components/SlotGrid.module.css
      provides: "slotGrid layout and layout toggle switch styles"
      contains: "slotGrid"
    - path: src/app/gameroom/components/UnifiedInputForm.module.css
      provides: "Input form container, input field, mode variants, focus trace animation"
      contains: "unifiedInputFormOnly"
  key_links:
    - from: src/app/gameroom/components/SlotGrid.tsx
      to: src/app/gameroom/components/SlotGrid.module.css
      via: "import styles from './SlotGrid.module.css'"
      pattern: "SlotGrid\\.module\\.css"
    - from: src/app/gameroom/components/UnifiedInputForm.tsx
      to: src/app/gameroom/components/UnifiedInputForm.module.css
      via: "import styles from './UnifiedInputForm.module.css'"
      pattern: "UnifiedInputForm\\.module\\.css"
---

<objective>
Extract SlotGrid and UnifiedInputForm styles from `gameroom.module.css` into their own modules. These two components are grouped in one plan because each has a small class footprint (~10 classes each), making this right-sized at 2 tasks.

Purpose: CleanUp the layout toggle and input form styles — both small and self-contained.
Output: SlotGrid.module.css, UnifiedInputForm.module.css, both tsx files updated.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SlotGrid.module.css and update import</name>
  <files>src/app/gameroom/components/SlotGrid.module.css, src/app/gameroom/components/SlotGrid.tsx</files>
  <action>
Create `src/app/gameroom/components/SlotGrid.module.css` with the following classes extracted verbatim from `gameroom.module.css`:

**Slot grid layout (lines 488–496):**
- `.slotGrid` — CSS grid, 3 columns, 2 rows auto, gap 1.5rem, width 100%

Also include the responsive overrides for `.slotGrid`:
- From `@media (max-width: 1024px)`: `.slotGrid` → 3 columns
- From `@media (max-width: 768px)`: `.slotGrid` → 2 columns
- From `@media (max-width: 768px)`: `.questionMark` → 2.5rem — NOTE: `.questionMark` belongs to SlotTile.module.css (Plan 03), but the 768px override is within the same media block. Do NOT include questionMark here — it will be included in SlotTile.module.css. Only include slotGrid responsive overrides in this file.

**Layout toggle (lines 1673–1739):**
- `.layoutToggleRow` — flex, align-items center, gap 0.75rem, margin-bottom 1.25rem
- `.layoutToggleLabel` — retro-font, 0.7rem, letter-spacing 2px, muted white uppercase
- `.layoutToggleBtn` — transparent bg, muted border/color, padding, retro-font, cursor pointer, transition
- `.layoutToggleBtn.layoutToggleActive` — neon-pink border/color, text-shadow, box-shadow
- `.layoutToggleTrack` — 42×22px pill, light bg, border, relative, cursor pointer, transition
- `.layoutToggleThumb` — absolute 14×14px circle, muted white
- `.layoutToggleThumb.layoutToggleThumbOn` — translateX(20px), neon-pink background + glow

In `SlotGrid.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./SlotGrid.module.css"`.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "SlotGrid" || echo "no SlotGrid errors"</automated>
  </verify>
  <done>SlotGrid.module.css exists with slotGrid grid layout, responsive overrides, and all 7 layoutToggle classes. SlotGrid.tsx imports from ./SlotGrid.module.css.</done>
</task>

<task type="auto">
  <name>Task 2: Create UnifiedInputForm.module.css and update import</name>
  <files>src/app/gameroom/components/UnifiedInputForm.module.css, src/app/gameroom/components/UnifiedInputForm.tsx</files>
  <action>
Create `src/app/gameroom/components/UnifiedInputForm.module.css` with the following classes extracted verbatim from `gameroom.module.css`:

**Input form container (lines 730–758):**
- `.unifiedInputFormOnly` — backdrop-filter, flex, gap 1rem, width 100%, relative, transition
- `.unifiedInputFormOnly::before` — absolute inset pseudo for gradient border glow, background linear-gradient with gradientShift animation
- `.unifiedInputFormOnly:has(.chatMode)` — chat-mode container box-shadow override

**Answer area container (lines 1468–1482):**
- `.answerAreaContainer` — transition, overflow hidden
- `.answerAreaContainer.hidden` — max-height 0, opacity 0.3
- `.answerAreaContainer.visible` — max-height 200px, opacity 1

**Input field (lines 1390–1466):**
- `.unifiedInputForm` — padding, border-top
- `.unifiedInput` — flex 1, padding, border-radius, gradient padding-box/border-box background, white color, retro-font-alt, transition
- `.unifiedInput.answerMode` — font-size 1.1rem retro-font, purple-to-blue gradient
- `.unifiedInput.chatMode` — purple-to-cyan gradient
- `.unifiedInput:focus` — no outline, caret-color #b700ff, conic-gradient border, traceSpot animation
- `.unifiedInput.answerMode:focus` — purple-blue conic-gradient variant
- `@property --spot-angle` — custom property declaration (syntax: "<angle>", initial-value: 0deg, inherits: false)
- `@keyframes traceSpot` — 0deg to 360deg for --spot-angle

Also include `@keyframes gradientShift` if it appears in gameroom.module.css (referenced by `.unifiedInputFormOnly::before`). Search the file for `@keyframes gradientShift` — if found, include it here.

In `UnifiedInputForm.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./UnifiedInputForm.module.css"`.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "UnifiedInputForm" || echo "no UnifiedInputForm errors"</automated>
  </verify>
  <done>UnifiedInputForm.module.css exists with form container, answer area, input field, and focus animation styles. UnifiedInputForm.tsx imports from ./UnifiedInputForm.module.css.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — no new errors. Confirm both module files exist: `ls src/app/gameroom/components/{SlotGrid,UnifiedInputForm}.module.css`.
</verification>

<success_criteria>
- SlotGrid.module.css created with grid and toggle styles
- UnifiedInputForm.module.css created with form container, input field, and animation styles
- Both tsx files import from their own modules
- TypeScript passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-04-SUMMARY.md`
</output>

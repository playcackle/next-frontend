---
phase: 06-gameroom-css-split
plan: "06"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
  - "06-05"
files_modified:
  - src/app/gameroom/gameroom.module.css
  - src/app/gameroom/page.tsx
  - src/app/gameroom/components/PostGameModal.module.css
  - src/app/gameroom/components/postgame.module.css
autonomous: false
requirements:
  - CSS-01
  - CSS-02
must_haves:
  truths:
    - "gameroom.module.css contains only page-level and layout styles — no rules that exclusively serve a split component"
    - "page.tsx renders without visual change: leaderboard tile, content row, slot container, post-game wrapper all appear correctly"
    - "PostGameModal.module.css and postgame.module.css serve distinct scopes — no class name is defined in both files doing the same job"
  artifacts:
    - path: src/app/gameroom/gameroom.module.css
      provides: "Page layout (container, main, contentRow, leaderboard, screen effects)"
      contains: "container"
    - path: src/app/gameroom/components/PostGameModal.module.css
      provides: "Modal slideshow overlay — PostGameModal component only"
      contains: "slideshow"
    - path: src/app/gameroom/components/postgame.module.css
      provides: "PostGameShowcase in-page panels — PostGameShowcase component only"
      contains: "showcaseContainer"
  key_links:
    - from: src/app/gameroom/page.tsx
      to: src/app/gameroom/gameroom.module.css
      via: "import styles from './gameroom.module.css'"
      pattern: "gameroom\\.module\\.css"
---

<objective>
Two things happen in this plan:

1. **CSS-01 cleanup**: Remove from `gameroom.module.css` every rule that was extracted into a component module in Plans 01–05. Only page-level and layout styles remain.

2. **CSS-02 rationalization**: Audit `PostGameModal.module.css` (415 lines, used by `PostGameModal.tsx`) and `postgame.module.css` (406 lines, used by `PostGameShowcase.tsx`). These two files have different class names and serve different components — PostGameModal is a full-screen slideshow overlay, postgame is an in-page showcase panel. Document in a comment block at the top of each file that they are intentionally separate with distinct scopes. No code changes needed if no duplicate class responsibilities exist.

Purpose: This is the cleanup pass. Plans 01–05 only added files and changed imports. This plan removes the now-redundant rules from the monolith and documents the post-game CSS split.
Output: Trimmed gameroom.module.css, documented post-game CSS files.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-gameroom-css-split/06-01-SUMMARY.md
@.planning/phases/06-gameroom-css-split/06-02-SUMMARY.md
@.planning/phases/06-gameroom-css-split/06-03-SUMMARY.md
@.planning/phases/06-gameroom-css-split/06-04-SUMMARY.md
@.planning/phases/06-gameroom-css-split/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip component-extracted rules from gameroom.module.css</name>
  <files>src/app/gameroom/gameroom.module.css</files>
  <action>
Remove from `gameroom.module.css` all rules that have been moved into component modules (Plans 01–05). Only keep rules that `page.tsx` still uses directly.

**Classes to REMOVE** (extracted to component modules):
- PlayerAvatar.module.css: `.avatarSmall`, `.avatarMedium`, `.avatarLarge`, `.playerAvatarImage`, `.playerAvatarGenerated` and compound selectors, hover rules
- RoomHeader.module.css: `.gameTitle`, `.roomName`, `.roomTitle` (both rule blocks), responsive overrides for gameTitle/roomName
- BotBobPinnedMessage.module.css: `.botBobPinned`, `.botBobPinned::before`, `.botBobPinnedContent`, `.botBobName`, `.botBobText`
- StatsRow.module.css: `.statsRow`, `.statsTile`, `.statsTileWrapper` and nested selectors, `.statsTileTooltip` and pseudo, `.statsTileWrapper:hover .statsTileTooltip`, `.timerWarning`, `@keyframes timerPulse`, `.statsValue`, `.playersCount`
- SlotTile.module.css: `.slotTile`, `.bonusTile`, `.answered`, `.answeredContent`, `.correctAnswer`, `.questionMark`, `.playerBadge` and pseudos, `.playerBadgeAvatar`, `.playerBadgeName`, `.correctPulse`, `.bonusTile.correctPulse`, `.successGlow`, `.successGlow.bonus`, `.slotTile.otherPlayerAnswering`, `.bonusTile.answered`, `@keyframes badgeAppear`, `@keyframes badgeShimmer`, `@keyframes avatarPulse`, `@keyframes pulse`, `@keyframes pulsePurple`, `@keyframes screenShake`, `@keyframes otherPlayerAnswering`, `@keyframes bonusGlow`, `@keyframes successGlowPulse`
- SlotGrid.module.css: `.slotGrid`, `.layoutToggleRow`, `.layoutToggleLabel`, `.layoutToggleBtn`, `.layoutToggleBtn.layoutToggleActive`, `.layoutToggleTrack`, `.layoutToggleThumb`, `.layoutToggleThumb.layoutToggleThumbOn`, slotGrid responsive overrides
- UnifiedInputForm.module.css: `.unifiedInputFormOnly` and pseudo/has selector, `.answerAreaContainer` and variants, `.unifiedInputForm`, `.unifiedInput` and mode/focus variants, `@property --spot-angle`, `@keyframes traceSpot`
- UnifiedMessages.module.css: `.unifiedMessagesContainer`, `.messagesScrollArea` and webkit scrollbar rules, `.messagesEmpty`, `.unifiedMessage` (both instances), `.chatMessage`, `.answerMessage`, `.successfulAnswerMessage`, `.messagePrefix`, `.messageUser`, `.messageTime`, `.messageContent` (both), `.canonicalAnswer`, `.ownMessage` (both instances), `.messageContentWrapper`, `.botBobMessage`, `.duplicateMessage`, `.ownSuccessfulAnswerMessage`, `:global(.performance-mode)` rules, `@keyframes messageAppear`, `@keyframes correctAnswerGlow`

**Classes to KEEP** (used by `page.tsx` directly):
- `.container` — page root wrapper
- `.main` — main content area with z-index and overflow
- `.contentRow` — grid layout 4fr 3fr 2fr
- `.postGameShowcaseWrapper` — grid-column 2/4
- `.slotContainer` — slot area background/border/shadow
- `.leaderboardTile` — in-game leaderboard column
- `.ingameLeaderboard` — scrollable player list in leaderboard tile
- `.leaderboardPlayer` — individual player row (flex, hover transition)
- `.leaderboardPlayer:hover` — hover state
- `.leaderboardPlayer:first-child .playerRank`, nth-child(2), nth-child(3) — gold/silver/bronze rank
- `.playerAvatar` — small 24×24px inline avatar in page.tsx leaderboard (NOT the PlayerAvatar component)
- `.playerAvatar::after` — glass highlight pseudo
- `.leaderboardItem:hover .playerAvatar` — scale on hover
- `.playerInfo` — name+score column layout
- `.playerName` — nowrap truncated name
- `.playerScore` — neon-pink score
- `.playerRank` — rank circle (standalone)
- `.scores` — bold pink score
- `.statsTitle` — shared label (used by page.tsx leaderboard header; keep in gameroom.module.css)
- `.rankUp`, `.rankDown`, `@keyframes rankUpAnimation`, `@keyframes rankDownAnimation` — leaderboard animations
- `.nameFlash`, `@keyframes nameFlash` — leaderboard name highlight
- `.answerRow` — answer form positioning wrapper
- `.shake`, `@keyframes shake` — page-level shake
- `.colorFlash`, `@keyframes colorFlash` — page-level color cycle
- `.zoomEffect`, `@keyframes zoomEffect` — page-level zoom
- `.rotateEffect`, `@keyframes rotateEffect` — page-level rotation
- `.colorBurstOverlay`, `.colorBurstOverlay.bonus`, `@keyframes colorBurst` — full-screen overlay
- `.particleContainer`, `.particle`, `.particlePurple`, `@keyframes particleAnimation` — particle explosion
- `@keyframes fullScreenShake` — full-screen shake
- `.screenShake` class + `@keyframes screenShake` — applied to `.main` in page.tsx via `animationState.shake`. Even though screenShake keyframes moved to SlotTile.module.css, `page.tsx` uses `styles.screenShake` from gameroom.module.css. Duplicate the `@keyframes screenShake` declaration here so the class continues to work.

**Responsive media queries**: Remove responsive overrides for deleted classes. Keep only overrides for retained classes (.main padding, .statsValue at 768px, .answerForm at 768px).

After removing, the file should be roughly 300–450 lines (down from 1,739).
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>gameroom.module.css no longer contains any of the 8 component-module class blocks. File is under 500 lines. TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rationalize and document post-game CSS files (CSS-02)</name>
  <files>src/app/gameroom/components/PostGameModal.module.css, src/app/gameroom/components/postgame.module.css</files>
  <action>
Audit `PostGameModal.module.css` and `postgame.module.css` for overlapping class responsibilities.

**PostGameModal.module.css** (used by `PostGameModal.tsx`) — slideshow modal overlay. Classes: overlay, content, slideshow, slide, navigation, navButton, progressDot, completedDot, activeDot, accoladeTitle, subtitle, playerCard, playerName, playerScore, icon, iconContainer, slideCounter, slideIn, skipButton, color variants (blue/brown/gold/green/pink/purple), titleblue/titlebrown/titlegold.

**postgame.module.css** (used by `PostGameShowcase.tsx`) — in-page showcase panels. Classes: showcaseContainer, panel, panelTitle, leaderboardPanel, entriesContainer, entry, rank, info, username, score, accoladesRow, accoladeChip, accoladeOverflow, accoladeIcon, accoladeCount, awardsPanel, awardsContainer, awardCard, awardTitle, awardWinner, awardWinnerName, awardWinnerCount, noAwardWinner, trophyIcon, scrollbar styles, accoladePopover, accoladePopoverTitle, accoladePopoverDescription.

These files have entirely different class names and serve different components. No class name appears in both files doing the same job.

**Action:** Add a scoping comment block at the top of each file to make this intentional separation explicit.

At the top of `PostGameModal.module.css`, add:
```css
/*
 * PostGameModal.module.css
 * Scope: PostGameModal component (full-screen slideshow overlay)
 * Used by: src/app/gameroom/components/PostGameModal.tsx
 * Separate from: postgame.module.css (PostGameShowcase in-page panels)
 * CSS-02: These two files intentionally serve different post-game UI contexts.
 */
```

At the top of `postgame.module.css`, add:
```css
/*
 * postgame.module.css
 * Scope: PostGameShowcase component (in-page leaderboard and awards panels)
 * Used by: src/app/gameroom/components/PostGameShowcase.tsx
 * Separate from: PostGameModal.module.css (full-screen modal slideshow)
 * CSS-02: These two files intentionally serve different post-game UI contexts.
 */
```

No class changes are needed.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && head -8 src/app/gameroom/components/PostGameModal.module.css && head -8 src/app/gameroom/components/postgame.module.css</automated>
  </verify>
  <done>Both post-game CSS files have scoping comment blocks at the top. No duplicate class responsibilities exist between the two files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual regression check — game room renders identically</name>
  <action>Start dev server and visually verify game room renders identically to pre-split appearance across all components.</action>
  <what-built>
    The complete CSS split: 8 component modules created (Plans 01–05), gameroom.module.css trimmed to page-level styles only, post-game CSS rationalized with scope comments.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open the game room in the browser and navigate to an active game room URL
    3. Check StatsRow: stat tiles render with correct neon-blue border, tooltips appear on hover
    4. Check SlotGrid: tiles show question marks / points, layout toggle switches between TILES and GRID views
    5. Check UnifiedMessages: message list renders with purple scrollbar, correct border colors per message type
    6. Check chat input: gradient border animates on focus
    7. Check leaderboard (right column): players listed with gold/silver/bronze rank circles
    8. Check RoomHeader: room name displays in neon-pink glow
    9. If possible, claim a slot: SlotTile badge entrance animation and pulse fire correctly
    10. Confirm no visual regressions compared to pre-split appearance
  </how-to-verify>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | head -5 || echo 'TS passed'</automated>
  </verify>
  <done>Human confirms game room renders identically to pre-split. TypeScript passes.</done>
  <resume-signal>Type "approved" if game room renders identically, or describe any visual differences found</resume-signal>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes. `gameroom.module.css` line count is under 500 (from 1,739). Both PostGame CSS files have scope comments. No component tsx file still imports from `../gameroom.module.css` except `page.tsx`.
</verification>

<success_criteria>
- gameroom.module.css contains only layout and page-level styles (container, main, contentRow, leaderboard, screen effects)
- No component (PlayerAvatar, StatsRow, UnifiedInputForm, BotBobPinnedMessage, RoomHeader, SlotTile, SlotGrid, UnifiedMessages) imports from gameroom.module.css
- PostGameModal.module.css and postgame.module.css have scope comments confirming intentional separation
- Game room renders identically in browser (human verified)
- TypeScript passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-06-SUMMARY.md`
</output>

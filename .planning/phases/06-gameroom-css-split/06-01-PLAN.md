---
phase: 06-gameroom-css-split
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/PlayerAvatar.module.css
  - src/app/gameroom/components/PlayerAvatar.tsx
  - src/app/gameroom/components/RoomHeader.module.css
  - src/app/gameroom/components/RoomHeader.tsx
  - src/app/gameroom/components/BotBobPinnedMessage.module.css
  - src/app/gameroom/components/BotBobPinnedMessage.tsx
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "PlayerAvatar renders image and generated variants at all three sizes without visual change"
    - "RoomHeader displays the room name with neon-pink glow without visual change"
    - "BotBobPinnedMessage shows the Bot Bob name in cyan and pinned text in bold white without visual change"
  artifacts:
    - path: src/app/gameroom/components/PlayerAvatar.module.css
      provides: "Avatar size classes and image/generated avatar styles"
      contains: "avatarSmall"
    - path: src/app/gameroom/components/RoomHeader.module.css
      provides: "Room title and room name neon styles"
      contains: "roomTitle"
    - path: src/app/gameroom/components/BotBobPinnedMessage.module.css
      provides: "Pinned bot message border, name, and text styles"
      contains: "botBobPinned"
  key_links:
    - from: src/app/gameroom/components/PlayerAvatar.tsx
      to: src/app/gameroom/components/PlayerAvatar.module.css
      via: "import styles from './PlayerAvatar.module.css'"
      pattern: "PlayerAvatar\\.module\\.css"
    - from: src/app/gameroom/components/RoomHeader.tsx
      to: src/app/gameroom/components/RoomHeader.module.css
      via: "import styles from './RoomHeader.module.css'"
      pattern: "RoomHeader\\.module\\.css"
    - from: src/app/gameroom/components/BotBobPinnedMessage.tsx
      to: src/app/gameroom/components/BotBobPinnedMessage.module.css
      via: "import styles from './BotBobPinnedMessage.module.css'"
      pattern: "BotBobPinnedMessage\\.module\\.css"
---

<objective>
Extract the styles for PlayerAvatar, RoomHeader, and BotBobPinnedMessage from `gameroom.module.css` into their own co-located CSS modules. Update each component's import. Do not change any CSS property values — this is a move, not a rewrite.

Purpose: Three self-contained, small components with no shared class concerns. Ideal first batch.
Output: Three new `*.module.css` files, three updated `*.tsx` imports.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlayerAvatar.module.css and update import</name>
  <files>src/app/gameroom/components/PlayerAvatar.module.css, src/app/gameroom/components/PlayerAvatar.tsx</files>
  <action>
Create `src/app/gameroom/components/PlayerAvatar.module.css` containing exactly these classes extracted verbatim from `gameroom.module.css` (lines 1496–1570):

- `.avatarSmall` — 28×28px sizing with margins
- `.avatarMedium` — 40×40px sizing
- `.avatarLarge` — 56×56px sizing
- `.playerAvatarImage` — circular, border, box-shadow, hover scale
- `.playerAvatarGenerated` — circular flex, initials font, hover scale
- `.avatarSmall.playerAvatarGenerated` — 0.6rem font
- `.avatarMedium.playerAvatarGenerated` — 0.9rem font
- `.avatarLarge.playerAvatarGenerated` — 1.2rem font
- `.playerAvatarGenerated:hover` — scale(1.05) box-shadow

Do NOT include `.playerAvatar` (the small leaderboard avatar — lines 402–460) — that belongs to the leaderboard in `page.tsx`.

In `PlayerAvatar.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./PlayerAvatar.module.css"`.

Do not remove the classes from `gameroom.module.css` yet — that happens in Plan 06.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "PlayerAvatar" || echo "no PlayerAvatar errors"</automated>
  </verify>
  <done>PlayerAvatar.module.css exists with all 9 class blocks. PlayerAvatar.tsx imports from ./PlayerAvatar.module.css. TypeScript reports no errors for PlayerAvatar.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Create RoomHeader.module.css and update import</name>
  <files>src/app/gameroom/components/RoomHeader.module.css, src/app/gameroom/components/RoomHeader.tsx</files>
  <action>
Create `src/app/gameroom/components/RoomHeader.module.css` containing exactly these classes extracted verbatim from `gameroom.module.css` (lines 25–73):

- `.gameTitle` — 2rem retro font, neon-blue text-shadow
- `.roomName` — 1.8rem retro font, room-color/neon-pink text-shadow
- `.roomTitle` (both rule blocks: text-align center with h1 selector, AND the inline-block pill with border) — NOTE: `.roomTitle` appears twice in gameroom.module.css (lines 51–53 and 63–73). Include both blocks.

Also include the responsive overrides that reference these classes from the `@media (max-width: 1024px)` and `@media (max-width: 768px)` blocks:
- `.gameTitle` at 1.5rem (1024px) and 1.2rem (768px)
- `.roomName` at 1.2rem (1024px) and 1rem (768px)

In `RoomHeader.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./RoomHeader.module.css"`.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "RoomHeader" || echo "no RoomHeader errors"</automated>
  </verify>
  <done>RoomHeader.module.css exists with gameTitle, roomName, and both roomTitle blocks plus responsive overrides. RoomHeader.tsx imports from ./RoomHeader.module.css.</done>
</task>

<task type="auto">
  <name>Task 3: Create BotBobPinnedMessage.module.css and update import</name>
  <files>src/app/gameroom/components/BotBobPinnedMessage.module.css, src/app/gameroom/components/BotBobPinnedMessage.tsx</files>
  <action>
Create `src/app/gameroom/components/BotBobPinnedMessage.module.css` containing exactly these classes extracted verbatim from `gameroom.module.css` (lines 1572–1614):

- `.botBobPinned` — border-bottom, padding, relative, overflow hidden
- `.botBobPinned::before` — absolute pseudo with left:-100% (shimmer sweep position)
- `.botBobPinnedContent` — flex:1, column, gap
- `.botBobName` — 0.85rem cyan (#00bfff) text-shadow retro-font uppercase
- `.botBobText` — bold 1.1rem white retro-font-alt

In `BotBobPinnedMessage.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./BotBobPinnedMessage.module.css"`.

Do not remove from `gameroom.module.css` yet.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "BotBob" || echo "no BotBob errors"</automated>
  </verify>
  <done>BotBobPinnedMessage.module.css exists with all 5 class/pseudo blocks. BotBobPinnedMessage.tsx imports from ./BotBobPinnedMessage.module.css.</done>
</task>

</tasks>

<verification>
After all three tasks: run `npx tsc --noEmit` from project root — zero errors for the three affected component files. Confirm each new module file exists: `ls src/app/gameroom/components/{PlayerAvatar,RoomHeader,BotBobPinnedMessage}.module.css`.
</verification>

<success_criteria>
- Three new module CSS files created, each containing only their component's styles
- Three component tsx files updated to import from their own module
- TypeScript compilation passes with no new errors
- gameroom.module.css is NOT modified in this plan (cleanup deferred to Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-01-SUMMARY.md`
</output>

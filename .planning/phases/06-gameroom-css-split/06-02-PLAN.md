---
phase: 06-gameroom-css-split
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/StatsRow.module.css
  - src/app/gameroom/components/StatsRow.tsx
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "StatsRow renders all five stat tiles with correct styling and tooltips"
    - "Timer warning turns yellow with pulse animation when time <= 30s"
    - "Tooltips appear on hover above each stat tile with the correct arrow and backdrop blur"
  artifacts:
    - path: src/app/gameroom/components/StatsRow.module.css
      provides: "Stats tile layout, tooltip, timer warning, and shared statsTitle/statsValue styles"
      contains: "statsTile"
  key_links:
    - from: src/app/gameroom/components/StatsRow.tsx
      to: src/app/gameroom/components/StatsRow.module.css
      via: "import styles from './StatsRow.module.css'"
      pattern: "StatsRow\\.module\\.css"
---

<objective>
Extract StatsRow styles from `gameroom.module.css` into `StatsRow.module.css`. Update the import in `StatsRow.tsx`. Copy verbatim — no property changes.

Purpose: StatsRow owns a well-defined section of the CSS (tiles, tooltip, timer). Clean extraction with one shared concern noted.
Output: StatsRow.module.css, updated StatsRow.tsx.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatsRow.module.css and update import</name>
  <files>src/app/gameroom/components/StatsRow.module.css, src/app/gameroom/components/StatsRow.tsx</files>
  <action>
Create `src/app/gameroom/components/StatsRow.module.css` with the following classes extracted verbatim from `gameroom.module.css`:

**Stats row layout (lines 75–108):**
- `.statsRow` — flex row, centered, clamp gap, clamp height
- `.statsTile` — dark bg, neon-blue border/shadow, flex column, min-width 19%

**Shared value/label classes (lines 184–199):**
- `.statsValue` — 1.5rem bold white neon-blue text-shadow retro-font
- `.playersCount` — 0.8rem muted uppercase (not used in current StatsRow.tsx but belongs here conceptually)

**Stats title** — `.statsTitle` is used by both `StatsRow.tsx` and `page.tsx`. Copy it into this module. Note in a CSS comment: `/* Also used by page.tsx leaderboard — will be duplicated in gameroom.module.css until Plan 06 cleanup */`
- `.statsTitle` — if this class exists in gameroom.module.css, include it (it is referenced as `styles.statsTitle` in StatsRow.tsx)

**Tooltip (lines 109–169):**
- `.statsTileWrapper` — relative flex, full height, min-width 19%
- `.statsTileWrapper .statsTile` — min-width unset, flex:1
- `.statsTileTooltip` — absolute positioned above, backdrop-blur, opacity/visibility transition
- `.statsTileTooltip::after` — triangle arrow pseudo-element
- `.statsTileWrapper:hover .statsTileTooltip` — opacity 1, visible

**Timer warning (lines 201–219):**
- `.timerWarning` — yellow color with timerPulse animation
- `@keyframes timerPulse` — text-shadow pulse 0/50/100%

In `StatsRow.tsx`: change `import styles from "../gameroom.module.css"` to `import styles from "./StatsRow.module.css"`.

Do not remove from `gameroom.module.css` yet (Plan 06 handles cleanup).
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | grep -E "StatsRow" || echo "no StatsRow errors"</automated>
  </verify>
  <done>StatsRow.module.css exists containing all tile, tooltip, and timer warning styles. StatsRow.tsx imports from ./StatsRow.module.css. TypeScript reports no errors for StatsRow.tsx.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — no new errors. Confirm `src/app/gameroom/components/StatsRow.module.css` exists and contains `.timerWarning` and `.statsTileTooltip`.
</verification>

<success_criteria>
- StatsRow.module.css created with all stats tile, tooltip, and timer warning styles
- StatsRow.tsx import updated
- TypeScript passes
- gameroom.module.css not modified yet
</success_criteria>

<output>
After completion, create `.planning/phases/06-gameroom-css-split/06-02-SUMMARY.md`
</output>

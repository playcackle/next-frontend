---
phase: 08-bug-fixes-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/page.tsx
autonomous: true
requirements:
  - BUG-01
  - PERF-02

must_haves:
  truths:
    - "Game room page loads without a React invariant violation — no hooks called after a conditional return"
    - "page.tsx no longer calls useGameState() for updateGameState — it uses useSetAtom(updateGameStateAtom) directly"
  artifacts:
    - path: "src/app/gameroom/page.tsx"
      provides: "Fixed hook ordering and granular atom subscription"
      contains: "useSetAtom(updateGameStateAtom)"
  key_links:
    - from: "src/app/gameroom/page.tsx"
      to: "src/app/gameroom/store/gameAtoms.ts"
      via: "useSetAtom(updateGameStateAtom)"
      pattern: "useSetAtom\\(updateGameStateAtom\\)"
---

<objective>
Fix the Rules of Hooks violation in page.tsx (BUG-01) and replace the full-state useGameState() subscription with a granular useSetAtom(updateGameStateAtom) (PERF-02 for page.tsx).

Purpose: The current code calls useGameEvents() and useChatSocket() after a conditional return, which is a React invariant violation that will crash in strict mode and React 19.
Output: page.tsx with all hooks unconditionally called at the top level; conditional render moved below all hook calls.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/gameroom/page.tsx
@src/app/gameroom/store/gameAtoms.ts
@src/app/gameroom/hooks/useGameEvents.ts
@src/app/gameroom/hooks/useChatWs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Rules of Hooks and replace useGameState in page.tsx</name>
  <files>src/app/gameroom/page.tsx</files>
  <action>
Apply two changes to src/app/gameroom/page.tsx:

**Change 1 — BUG-01: Move all hooks before the conditional return.**

Current structure (lines 98–116):
```
const { submitAnswer } = useGameActions();      // line 99

if (!gameroom) {                                 // line 101
  return <div>Loading gameroom...</div>;
}

const { sendEvent } = useGameEvents(...);        // line 106
function getBaseWsUrl(...) { ... }               // line 109
const baseWsUrl = getBaseWsUrl(...);             // line 112
const { sendMessage: sendChatMessage } = useChatSocket(...); // line 113
const handleUnifiedSubmit = (...) => { ... };   // line 119
```

Target structure:
1. Move `const { sendEvent } = useGameEvents(gameroom?.game_ws_url ?? "", gameroom?.token ?? "")` to BEFORE the `if (!gameroom)` guard (after the `useGameActions()` call at line 99).
2. Move `const { sendMessage: sendChatMessage } = useChatSocket(...)` similarly before the guard. Use `gameroom?.game_ws_url ?? ""` and `gameroom?.token ?? ""` as arguments since gameroom may be null at hook call time.
3. Move `getBaseWsUrl` helper function definition and `baseWsUrl` constant before the guard.
4. Move `handleUnifiedSubmit` before the guard (it references `sendEvent`, `sendChatMessage`, `submitAnswer`, `addAnswerBubble` which are all available by this point).
5. Leave the `if (!gameroom) { return <div>Loading gameroom...</div>; }` guard in place — just ensure no hooks are called after it.
6. The rest of the component body (handleSoundsLoaded ref, onSoundsLoaded callback, return JSX) stays below the guard as before.

**Change 2 — PERF-02: Replace useGameState() with useSetAtom(updateGameStateAtom).**

Remove line 56: `const { updateGameState } = useGameState();`
Add instead: `const updateGameState = useSetAtom(updateGameStateAtom);`

Update the import at the top of the file:
- Remove `useGameState` from the `./hooks/useGameState` import (or remove the entire import if it is no longer used).
- Add `updateGameStateAtom` to the existing named import from `./store/gameAtoms` (it is already imported from there for other atoms).
- Ensure `useSetAtom` is imported from `jotai` (it is already imported — check the import line).

After both changes, the hook call order in the component body must be:
1. `useAtomValue(gameRoomAtom)` — existing
2. `useAtomValue(animationStateAtom)` — existing
3. `useAnswerBubbles()` — existing
4. `useAtomValue(loadingAtom)` — existing
5. `useAtomValue(isRoundBreakAtom)` — existing
6. `useAtomValue(isPostGameShowcaseAtom)` — existing
7. `useAtomValue(timeRemainingAtom)` — existing
8. `useAtomValue(showCountDownAtom)` — existing
9. `useAtomValue(scoresAtom)` — existing
10. `useAtomValue(roundNameAtom)` — existing
11. `useSetAtom(updateGameStateAtom)` — replaces useGameState()
12. `useRef(...)` calls — existing
13. `useState(...)` call — existing
14. `useEffect(...)` call — existing
15. `useGameActions()` — existing
16. `useGameEvents(gameroom?.game_ws_url ?? "", gameroom?.token ?? "")` — MOVED here
17. getBaseWsUrl helper + baseWsUrl constant — MOVED here
18. `useChatSocket(baseWsUrl, gameroom?.token ?? "")` — MOVED here
19. `handleUnifiedSubmit` function — MOVED here
20. `if (!gameroom) return <div>Loading gameroom...</div>;` — guard stays
21. `useRef(false)` for handleSoundsLoaded — existing
22. `useCallback` for onSoundsLoaded — existing
23. return JSX — existing
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
TypeScript compiles without errors in page.tsx. All hook calls appear before the `if (!gameroom)` conditional return. `useGameState` import is removed from page.tsx. `useSetAtom(updateGameStateAtom)` is used instead.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from the project root — zero type errors.
Grep confirms no `useGameState` import remains in page.tsx: `grep "useGameState" src/app/gameroom/page.tsx` returns nothing.
Grep confirms hooks before guard: `useGameEvents` and `useChatSocket` appear before the `if (!gameroom)` line.
</verification>

<success_criteria>
1. `npx tsc --noEmit` passes with no errors related to page.tsx.
2. `grep -n "useGameEvents\|useChatSocket\|if (!gameroom)" src/app/gameroom/page.tsx` shows useGameEvents and useChatSocket at lower line numbers than the guard.
3. `grep "useGameState" src/app/gameroom/page.tsx` returns empty.
4. `grep "useSetAtom(updateGameStateAtom)" src/app/gameroom/page.tsx` returns a match.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes-and-performance/08-01-SUMMARY.md`
</output>

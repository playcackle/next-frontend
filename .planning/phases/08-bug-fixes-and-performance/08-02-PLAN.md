---
phase: 08-bug-fixes-and-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/gameroom/components/AnswerReveal.tsx
autonomous: true
requirements:
  - BUG-02
  - PERF-02

must_haves:
  truths:
    - "Answer reveal animation fires — styles.visible is applied when a slot id is in visibleAnswers"
    - "AnswerReveal no longer subscribes to full gameStateAtom — it reads only slotsAtom"
  artifacts:
    - path: "src/app/gameroom/components/AnswerReveal.tsx"
      provides: "Fixed type alignment and granular atom subscription"
      contains: "useAtomValue(slotsAtom)"
  key_links:
    - from: "src/app/gameroom/components/AnswerReveal.tsx"
      to: "src/app/gameroom/store/gameAtoms.ts"
      via: "useAtomValue(slotsAtom)"
      pattern: "useAtomValue\\(slotsAtom\\)"
---

<objective>
Fix the type mismatch in AnswerReveal.tsx that prevents the animation from ever firing (BUG-02) and replace the full-state useGameState() subscription with useAtomValue(slotsAtom) (PERF-02 for AnswerReveal).

Purpose: `QuizAnswer.id` is typed as `number` but Slot.id is a `string`. `visibleAnswers.includes(x.id)` comparing `number[]` against `string` always returns false so `styles.visible` is never applied and the reveal animation never runs.
Output: AnswerReveal.tsx with aligned string ids, string[] visibleAnswers state, no type cast, and a direct slotsAtom subscription.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/gameroom/components/AnswerReveal.tsx
@src/app/gameroom/store/gameAtoms.ts
@src/app/gameroom/hooks/useGameState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AnswerReveal type mismatch and replace useGameState subscription</name>
  <files>src/app/gameroom/components/AnswerReveal.tsx</files>
  <action>
Apply three changes to src/app/gameroom/components/AnswerReveal.tsx:

**Change 1 — Fix QuizAnswer.id type:**
Change the local `QuizAnswer` type definition:
```typescript
// Before
type QuizAnswer = {
  id: number;
  ...
};

// After
type QuizAnswer = {
  id: string;
  ...
};
```

**Change 2 — Fix visibleAnswers state type:**
Change the useState declaration:
```typescript
// Before
const [visibleAnswers, setVisibleAnswers] = useState<number[]>([]);

// After
const [visibleAnswers, setVisibleAnswers] = useState<string[]>([]);
```

**Change 3 — Replace useGameState with useAtomValue(slotsAtom) and remove the type cast:**
Remove the import of `useGameState` from `../hooks/useGameState`.
Add imports for `useAtomValue` from `jotai` and `slotsAtom` from `../store/gameAtoms`.

Replace:
```typescript
const { slots } = useGameState();
```
With:
```typescript
const slots = useAtomValue(slotsAtom);
```

In the `useEffect` that maps slots to answers, remove the `as unknown as QuizAnswer` cast:
```typescript
// Before
} as unknown as QuizAnswer)

// After
} as QuizAnswer)
```

This cast is safe to remove because after Change 1 `QuizAnswer.id` is now `string`, which matches `Slot.id` (also `string`), so the shape is compatible.

After all changes, the `visibleAnswers.includes(x.id)` call on line 62 will correctly compare `string[]` against `string`, and `styles.visible` will be applied when expected.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
TypeScript compiles without errors. `grep "useGameState" src/app/gameroom/components/AnswerReveal.tsx` returns empty. `grep "slotsAtom" src/app/gameroom/components/AnswerReveal.tsx` returns a match. `grep "number\[\]" src/app/gameroom/components/AnswerReveal.tsx` returns empty (visibleAnswers is string[]).
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with no errors.
`grep "useGameState" src/app/gameroom/components/AnswerReveal.tsx` is empty.
`grep "slotsAtom" src/app/gameroom/components/AnswerReveal.tsx` shows the import and usage.
`grep "number\[\]" src/app/gameroom/components/AnswerReveal.tsx` is empty.
`grep "as unknown as" src/app/gameroom/components/AnswerReveal.tsx` is empty.
</verification>

<success_criteria>
1. `npx tsc --noEmit` passes.
2. `QuizAnswer.id` is `string` in the type definition.
3. `visibleAnswers` is `string[]`.
4. `useAtomValue(slotsAtom)` replaces `useGameState()`.
5. No `as unknown as QuizAnswer` cast remains.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes-and-performance/08-02-SUMMARY.md`
</output>

---
phase: 07-admin-route-css-tidy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/lobbies/[id]/page.module.css
  - src/app/admin/topics/[id]/page.module.css
  - src/app/admin/slots/[id]/page.module.css
  - src/app/collections/page.module.css
  - src/app/admin/collections/[id]/page.module.css
autonomous: true
requirements:
  - CSS-03

must_haves:
  truths:
    - "Each of the 5 target CSS files has clear /* ── Section ── */ navigation headers throughout"
    - "Every logical block of rules in each file is preceded by a dashed-style section comment"
    - "No CSS property values are changed — purely additive comment changes"
  artifacts:
    - path: "src/app/admin/lobbies/[id]/page.module.css"
      provides: "Navigable admin lobby edit page styles with section headers"
      contains: "/* ──"
    - path: "src/app/admin/topics/[id]/page.module.css"
      provides: "Navigable admin topic edit page styles with section headers"
      contains: "/* ──"
    - path: "src/app/admin/slots/[id]/page.module.css"
      provides: "Navigable admin slot edit page styles with section headers"
      contains: "/* ──"
    - path: "src/app/collections/page.module.css"
      provides: "Navigable collections list page styles with section headers"
      contains: "/* ──"
    - path: "src/app/admin/collections/[id]/page.module.css"
      provides: "Navigable admin collection edit page styles with section headers"
      contains: "/* ──"
  key_links: []
---

<objective>
Add clear section-comment navigation headers to the 5 single-consumer oversized CSS module files in admin and other routes that currently lack them.

Purpose: These files (450–601 lines) each serve a single page.tsx — splitting them would create artificial fragmentation with no component boundary to justify it. The navigability goal is met by adding /* ── Section Name ── */ dividers matching the project's established style (used in how-to-play/page.module.css and onboarding-modal.module.css). No CSS property values are changed.
Output: 5 files updated with section headers — readable from any editor position without scrolling the full file.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/admin/lobbies/[id]/page.module.css
@src/app/admin/topics/[id]/page.module.css
@src/app/admin/slots/[id]/page.module.css
@src/app/collections/page.module.css
@src/app/admin/collections/[id]/page.module.css

<interfaces>
<!-- Reference: established section comment style from how-to-play/page.module.css -->
/* ── Page wrapper ─────────────────────────────────────────── */
/* ── Scene container (UI + callouts) ─────────────────────── */
/* ── Header bar ──────────────────────────────────────────── */

<!-- The dashes fill to ~60 chars total. Use this pattern for all new headers. -->
<!-- Pattern: /* ── {Section Name} {dashes to fill} */ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section headers to admin/lobbies and admin/topics CSS files</name>
  <files>
    src/app/admin/lobbies/[id]/page.module.css
    src/app/admin/topics/[id]/page.module.css
  </files>
  <action>
    Read each file fully before editing. Add /* ── Section Name ─── */ headers before each logical group of rules. Do NOT change any property values, selectors, or rule bodies — only insert comment lines.

    For src/app/admin/lobbies/[id]/page.module.css (601 lines), the file already has some inline comments like "/* Loading & Error States */", "/* Apply Mode Section */", etc. Upgrade those to the dashed style and add any missing ones. Logical sections to label (derive from existing comments and class groupings):
    - Page wrapper / Header (container, header, backButton, title, neonText, subtitle, lobbyId)
    - Loading & Error States
    - Apply Mode Section
    - Section layout
    - Collection Controls
    - Parameter Grid
    - Radix Slider Styling
    - Slot Preview
    - Add any remaining unlabeled groups (check after the slider section)

    For src/app/admin/topics/[id]/page.module.css (537 lines), read the file and identify logical groups, then add matching dashed headers. Typical groups in topic admin pages: Page Header, Loading/Error States, Form layout, Field groups, Action buttons, Responsive rules.

    Section header format (pad dashes to ~60 chars total):
    /* ── Page Header ─────────────────────────────────────────── */
  </action>
  <verify>
    <automated>grep -c "^/\* ──" src/app/admin/lobbies/[id]/page.module.css src/app/admin/topics/[id]/page.module.css</automated>
  </verify>
  <done>
    Both files have at least 6 section headers each using the dashed comment style.
    grep -c "^/\* ──" returns ≥ 6 for each file.
    No CSS rules changed — diff shows only inserted comment lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section headers to admin/slots, collections, and admin/collections CSS files</name>
  <files>
    src/app/admin/slots/[id]/page.module.css
    src/app/collections/page.module.css
    src/app/admin/collections/[id]/page.module.css
  </files>
  <action>
    Read each file fully before editing. Add /* ── Section Name ─── */ headers before each logical group of rules. Do NOT change any property values, selectors, or rule bodies.

    For src/app/admin/slots/[id]/page.module.css (492 lines): identify logical groups (page header, form layout, field groups, slot preview, action buttons, responsive rules) and add dashed headers.

    For src/app/collections/page.module.css (468 lines): this file has NO existing comments at all. Read all class names, group them by visual/functional purpose (page wrapper, header, collection grid/list, collection card, filters/controls, empty state, responsive rules), then insert section headers before each group.

    For src/app/admin/collections/[id]/page.module.css (450 lines): same approach — read all class names, identify groups, insert dashed section headers.

    Section header format:
    /* ── {Section Name} ─────────────────────────────────────── */

    Pad trailing dashes so the comment line is approximately 60 characters wide (matching the project style in how-to-play/page.module.css).
  </action>
  <verify>
    <automated>grep -c "^/\* ──" src/app/admin/slots/[id]/page.module.css src/app/collections/page.module.css "src/app/admin/collections/[id]/page.module.css"</automated>
  </verify>
  <done>
    All three files have at least 5 section headers each using the dashed comment style.
    grep -c "^/\* ──" returns ≥ 5 for each file.
    No CSS rules changed — diff shows only inserted comment lines.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -c "^/\* ──" src/app/admin/lobbies/[id]/page.module.css` ≥ 6
- `grep -c "^/\* ──" src/app/admin/topics/[id]/page.module.css` ≥ 5
- `grep -c "^/\* ──" src/app/admin/slots/[id]/page.module.css` ≥ 5
- `grep -c "^/\* ──" src/app/collections/page.module.css` ≥ 5
- `grep -c "^/\* ──" "src/app/admin/collections/[id]/page.module.css"` ≥ 5
- `git diff --stat` shows only the 5 target files changed
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 5 files have clear section headers in the established project style
- No property values modified — purely additive comment insertion
- TypeScript build still passes
</success_criteria>

<output>
After completion, create .planning/phases/07-admin-route-css-tidy/07-02-SUMMARY.md
</output>

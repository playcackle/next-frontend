---
phase: 07-admin-route-css-tidy
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified: []
autonomous: false
requirements:
  - CSS-03

must_haves:
  truths:
    - "Home page renders with correct layout, hero section, gamerooms grid, leaderboard card, and user stats"
    - "Admin lobby edit page renders without visual regressions"
    - "Admin topic edit page renders without visual regressions"
    - "Collections page renders without visual regressions"
  artifacts: []
  key_links: []
---

<objective>
Verify that all CSS refactoring from Plans 01 and 02 produces no visual regressions. The home page had the most significant structural change (3 new CSS modules), so it is the primary verification target. Admin pages had comment-only changes so only spot-checking is needed.

Purpose: The executor cannot visually verify pages — human confirms that styles load correctly and the UI matches its pre-refactor state.
Output: Human confirmation that the phase is visually complete and correct.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admin-route-css-tidy/07-01-SUMMARY.md
@.planning/phases/07-admin-route-css-tidy/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build verification</name>
  <files></files>
  <action>
    Run the following checks and report results:

    1. TypeScript check: `npx tsc --noEmit`
    2. Verify no CSS module import errors by checking that all 3 new component modules exist and are non-empty:
       - `wc -l src/components/home-leaderboard.module.css`
       - `wc -l src/components/home-user-stats.module.css`
       - `wc -l src/components/home-gamerooms.module.css`
    3. Confirm page.module.css is trimmed:
       - `wc -l src/app/page.module.css` (expect under 250 lines)
    4. Confirm no component still imports from app/page.module.css:
       - `grep -rn "app/page\.module\.css" src/components/` (should return no matches)
    5. Confirm leaderboard page no longer imports page.module.css:
       - `grep -n "page\.module\.css" src/app/leaderboard/page.tsx` (should return no matches)

    Report all results before the checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeScript: PASS" || echo "TypeScript: FAIL"</automated>
  </verify>
  <done>TypeScript passes. All file size and import checks completed and reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual regression check — home page and admin pages</name>
  <files></files>
  <action>
    Start the dev server (npm run dev) and verify the pages listed in how-to-verify. Pause for human confirmation.
  </action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify instructions"</automated>
  </verify>
  <done>Human has confirmed no visual regressions and typed "approved".</done>
  <what-built>
    Phase 7 CSS refactoring:
    - src/app/page.module.css split into 3 per-component modules (home-leaderboard, home-user-stats, home-gamerooms)
    - 5 single-consumer admin/route CSS files received section navigation headers
    - leaderboard/page.tsx import cleaned up
  </what-built>
  <how-to-verify>
    Start the dev server (npm run dev) and check:

    1. HOME PAGE (http://localhost:3000)
       - Hero section renders with neon text "CACKLE" and correct glow effects
       - "Game Rooms" section shows the grid of lobby tiles (or empty state if no lobbies)
       - "Your Stats" section shows stat cards (or loading skeleton, then stats)
       - "Global Leaderboard" section shows the leaderboard card with period toggle buttons
       - Two-column layout on desktop, single column on narrow viewport

    2. LEADERBOARD PAGE (http://localhost:3000/leaderboard)
       - Page renders without blank layout (footer should still appear)

    3. ADMIN PAGES — spot-check one:
       - Visit any admin lobby edit page: http://localhost:3000/admin/lobbies/[any-id]
       - Confirm page renders correctly (no missing styles, layout intact)

    Report any visual issue or type "approved" to confirm no regressions.
  </how-to-verify>
  <resume-signal>Type "approved" to mark phase complete, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
- TypeScript build passes
- Human confirms home page renders correctly with all three sections styled as before
- Human confirms no visual regressions on admin pages
</verification>

<success_criteria>
- No visual regressions on home page (hero, gamerooms, stats, leaderboard sections all styled correctly)
- No visual regressions on admin pages
- TypeScript passes
- Phase 7 CSS-03 requirement verified as complete
</success_criteria>

<output>
After completion, create .planning/phases/07-admin-route-css-tidy/07-03-SUMMARY.md
</output>

---
phase: 07-admin-route-css-tidy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.module.css
  - src/components/home-leaderboard.module.css
  - src/components/home-user-stats.module.css
  - src/components/home-gamerooms.module.css
  - src/components/home-leaderboard.tsx
  - src/components/home-user-stats.tsx
  - src/components/home-gamerooms.tsx
  - src/app/leaderboard/page.tsx
autonomous: true
requirements:
  - CSS-03

must_haves:
  truths:
    - "home-leaderboard.tsx imports from its own module, not from app/page.module.css"
    - "home-user-stats.tsx imports from its own module, not from app/page.module.css"
    - "home-gamerooms.tsx imports from its own module, not from app/page.module.css"
    - "src/app/page.module.css contains only page-level layout classes (under 200 lines)"
    - "src/app/leaderboard/page.tsx no longer imports from ../page.module.css"
  artifacts:
    - path: "src/components/home-leaderboard.module.css"
      provides: "Leaderboard card, period toggle, leader list styles"
      exports: ["leaderboardCard", "periodToggle", "leaderList", "leaderRow"]
    - path: "src/components/home-user-stats.module.css"
      provides: "Stats grid, stat card, loading skeleton styles"
      exports: ["statsGrid", "statCard", "statValue", "statsLoadingRow", "statsSkeletonCard"]
    - path: "src/components/home-gamerooms.module.css"
      provides: "Gamerooms section, grid, status badge styles"
      exports: ["gameroomsSection", "gameroomsGrid", "browseAllBtn", "statusBadge"]
    - path: "src/app/page.module.css"
      provides: "Page wrapper, hero, auth error banner, layout grid"
      contains: "pageWrapper"
  key_links:
    - from: "src/components/home-leaderboard.tsx"
      to: "src/components/home-leaderboard.module.css"
      via: "import styles"
      pattern: "home-leaderboard\\.module\\.css"
    - from: "src/components/home-user-stats.tsx"
      to: "src/components/home-user-stats.module.css"
      via: "import styles"
      pattern: "home-user-stats\\.module\\.css"
    - from: "src/components/home-gamerooms.tsx"
      to: "src/components/home-gamerooms.module.css"
      via: "import styles"
      pattern: "home-gamerooms\\.module\\.css"
---

<objective>
Split src/app/page.module.css (601 lines, 5 consumers) into per-consumer CSS modules — same pattern used in Phase 6 for gameroom.module.css.

Purpose: page.module.css is shared by 3 home sub-components and 1 unrelated page (leaderboard), violating the CSS Modules principle of co-location. Each component owns its styles; the root page.tsx retains only page-level layout rules.
Output: 3 new CSS module files in src/components/, trimmed page.module.css, updated imports in 4 files.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.module.css
@src/app/page.tsx
@src/components/home-leaderboard.tsx
@src/components/home-user-stats.tsx
@src/components/home-gamerooms.tsx
@src/app/leaderboard/page.tsx

<interfaces>
<!-- Classes used by each consumer — extracted from codebase -->

home-leaderboard.tsx uses from page.module.css:
  .leaderboardCard, .periodToggle, .periodBtn, .periodBtnActive,
  .leaderList, .leaderRow, .leaderRowFirst, .leaderRank,
  .rank1, .rank2, .rank3, .leaderName, .leaderScore

home-user-stats.tsx uses from page.module.css:
  .statsGrid, .statCard, .statCard_blue, .statCard_pink, .statCard_purple, .statCard_green,
  .statValue, .statValue_blue, .statValue_pink, .statValue_purple, .statValue_green,
  .statLabel, .statsLoadingRow, .statsSkeletonCard, .statsError, .retryBtn
  (plus @keyframes skeleton)

home-gamerooms.tsx uses from page.module.css:
  .gameroomsSection, .gameroomsGrid, .gameroomsFooter, .gameroomsEmpty,
  .statusBadge, .status_open, .status_in_progress, .status_full, .browseAllBtn

leaderboard/page.tsx uses from ../page.module.css:
  .container (does not exist — no-op), .main (does not exist — no-op), .footer (line 570)
  Note: .container and .main are undefined references, only .footer applies.

page.tsx uses from page.module.css:
  .pageWrapper, .heroSection, .title, .neonText, .neonTextPink, .tagline,
  .authErrorBanner, .authErrorIcon, .authErrorBanner strong, .authErrorBanner p, .authErrorLink,
  .authSection, .twoColLayout, .section, .sectionHeader, .sectionTitle, .sectionTitleAccent,
  .seeAllLink, .seeAllLink:hover, .rightCol, .footer
  (plus @keyframes neonPulse, neonPulsePink, fadeIn)
  (plus @media (max-width: 900px), @media (max-width: 640px) — only .title, .twoColLayout rules)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create home-leaderboard.module.css and home-user-stats.module.css, update their imports</name>
  <files>
    src/components/home-leaderboard.module.css
    src/components/home-user-stats.module.css
    src/components/home-leaderboard.tsx
    src/components/home-user-stats.tsx
  </files>
  <action>
    Extract verbatim from src/app/page.module.css (no property changes):

    1. Create src/components/home-leaderboard.module.css containing these sections cut verbatim from page.module.css (lines 229-346):
       - /* ── Leaderboard card ─── */ block: .leaderboardCard, .periodToggle, .periodBtn, .periodBtnActive, .leaderList, .leaderRow, .leaderRow:hover, .leaderRowFirst, .leaderRank, .rank1, .rank2, .rank3, .leaderName, .leaderScore
       Add a top comment: /* Styles for home-leaderboard.tsx — extracted from page.module.css (Phase 7) */

    2. Create src/components/home-user-stats.module.css containing these sections cut verbatim from page.module.css (lines 348-486 approximately):
       - /* ── User stats grid ─── */ block: .statsGrid, .statCard, .statCard:hover, .statCard_blue/pink/purple/green (and their :hover), .statValue, .statValue_blue/pink/purple/green, .statLabel, .statsLoadingRow, .statsSkeletonCard, @keyframes skeleton, .statsError, .retryBtn, .retryBtn:hover
       Also include the responsive rule for .statsGrid and .statsLoadingRow from @media (max-width: 640px).
       Add a top comment: /* Styles for home-user-stats.tsx — extracted from page.module.css (Phase 7) */

    3. In src/components/home-leaderboard.tsx: change the import from
         import styles from "@/app/page.module.css";
       to
         import styles from "./home-leaderboard.module.css";

    4. In src/components/home-user-stats.tsx: change the import from
         import styles from "@/app/page.module.css";
       to
         import styles from "./home-user-stats.module.css";

    Extraction rule: copy CSS verbatim — no property values changed.
  </action>
  <verify>
    <automated>grep -n "page\.module\.css" src/components/home-leaderboard.tsx src/components/home-user-stats.tsx && echo "FAIL: still importing page.module.css" || echo "PASS: imports updated"</automated>
  </verify>
  <done>
    home-leaderboard.tsx and home-user-stats.tsx import from their own modules.
    home-leaderboard.module.css and home-user-stats.module.css exist and contain the correct classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create home-gamerooms.module.css, fix leaderboard/page.tsx, trim page.module.css</name>
  <files>
    src/components/home-gamerooms.module.css
    src/components/home-gamerooms.tsx
    src/app/leaderboard/page.tsx
    src/app/page.module.css
  </files>
  <action>
    1. Create src/components/home-gamerooms.module.css containing these sections cut verbatim from page.module.css:
       - /* ── Gamerooms section ─── */ block: .gameroomsSection, .gameroomsGrid, .gameroomsFooter, /* Status badge */ .statusBadge, .status_open, .status_in_progress, .status_full, .browseAllBtn, .browseAllBtn:hover, .gameroomsEmpty
       Also include the .gameroomsGrid responsive rule from @media (max-width: 640px).
       Add a top comment: /* Styles for home-gamerooms.tsx — extracted from page.module.css (Phase 7) */

    2. In src/components/home-gamerooms.tsx: change the import from
         import styles from "@/app/page.module.css";
       to
         import styles from "./home-gamerooms.module.css";

    3. Fix src/app/leaderboard/page.tsx:
       - The file imports `pageStyles from "../page.module.css"` but only uses .footer (which exists) plus .container and .main (which do NOT exist in page.module.css and resolve to undefined).
       - Change `className={pageStyles.container}` → `className={styles.container}` (use the existing leaderboard.module.css import aliased as `styles`)
       - Change `className={pageStyles.main}` → `className={styles.main}` (use styles)
       - For `className={pageStyles.footer}` → either add a .footer rule to leaderboard.module.css OR keep pageStyles.footer if .footer should remain shared. The safest approach: move the .footer rule from page.module.css to leaderboard.module.css (it is only referenced by leaderboard/page.tsx among non-home files) and update leaderboard/page.tsx to use `styles.footer`. Then remove the `import pageStyles` line entirely.
       - If .footer is also needed by page.tsx (the home page): check page.tsx usage. If page.tsx also uses styles.footer, keep .footer in page.module.css and leaderboard/page.tsx can define its own .footer locally.
       - IMPORTANT: Read page.tsx to confirm whether it uses styles.footer before deciding. If page.tsx uses styles.footer, create a duplicate .footer in leaderboard.module.css and update leaderboard/page.tsx to use its own styles import. Remove pageStyles import from leaderboard/page.tsx entirely.

    4. Remove from src/app/page.module.css the blocks now fully owned by the three component modules:
       - The leaderboard card block (/* ── Leaderboard card ─── */ through .leaderScore)
       - The user stats grid block (/* ── User stats grid ─── */ through .retryBtn hover)
       - The gamerooms section block (/* ── Gamerooms section ─── */ through .gameroomsEmpty)
       - Remove from @media (max-width: 640px): .statsGrid, .statsLoadingRow, .gameroomsGrid rules (moved to their respective modules)
       Result: page.module.css should be approximately 190-220 lines containing only page-level layout (pageWrapper, heroSection, title, neon text, auth error, authSection, twoColLayout, section/sectionHeader/sectionTitle, seeAllLink, rightCol, footer, responsive .twoColLayout and .title rules).
  </action>
  <verify>
    <automated>grep -n "page\.module\.css" src/components/home-gamerooms.tsx && echo "FAIL: still importing page.module.css" || echo "PASS: import updated"; wc -l src/app/page.module.css</automated>
  </verify>
  <done>
    home-gamerooms.tsx imports from ./home-gamerooms.module.css.
    src/app/page.module.css is under 250 lines (page-level layout only).
    src/app/leaderboard/page.tsx no longer imports from ../page.module.css.
    TypeScript build passes: npx tsc --noEmit exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rn "page\.module\.css" src/components/home-leaderboard.tsx src/components/home-user-stats.tsx src/components/home-gamerooms.tsx` returns no matches
- `wc -l src/app/page.module.css` shows under 250 lines
- `grep -n "pageStyles" src/app/leaderboard/page.tsx` returns no matches
- `npx tsc --noEmit` passes (no type errors)
- `ls src/components/home-leaderboard.module.css src/components/home-user-stats.module.css src/components/home-gamerooms.module.css` — all three exist
</verification>

<success_criteria>
- src/app/page.module.css contains only page-level layout classes, under 250 lines
- 3 new CSS module files exist in src/components/, one per home sub-component
- All 3 home component imports reference their own module file
- leaderboard/page.tsx no longer imports from ../page.module.css
- TypeScript build passes with no errors
</success_criteria>

<output>
After completion, create .planning/phases/07-admin-route-css-tidy/07-01-SUMMARY.md
</output>

---
phase: 05-codebase-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "Code quality findings are documented with exact file locations and line numbers"
    - "Performance findings are documented covering re-renders, bundle, animation overhead, and slow paths"
    - "Each finding has an impact rating (HIGH/MEDIUM/LOW)"
    - "A draft findings file exists that Plan 02 can consume for consolidation"
  artifacts:
    - path: ".planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md"
      provides: "Code quality and performance findings with impact ratings"
      contains: "## Code Quality\n## Performance"
  key_links:
    - from: "src/components/sound-effects.tsx"
      to: "05-01-FINDINGS-DRAFT.md"
      via: "Direct file reading — LOC count, structure review"
    - from: "src/app/gameroom/hooks/useGameActions.ts"
      to: "05-01-FINDINGS-DRAFT.md"
      via: "Direct file reading — animation bypass, DOM manipulation"
    - from: "src/app/gameroom/page.tsx"
      to: "05-01-FINDINGS-DRAFT.md"
      via: "Direct file reading — atom subscriptions, inline leaderboard"
---

<objective>
Conduct audit passes 1 and 2: code quality and performance.

Purpose: Read the gameroom components, hooks, and shared components to verify and document all code quality findings (AUDIT-01) and performance findings (AUDIT-02) identified in the research. Produce a structured draft findings file that Plan 02 will use for final consolidation.

Output: `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md` with two complete sections — Code Quality and Performance — each containing verified findings with exact file locations, line numbers, and impact ratings.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codebase-audit/05-RESEARCH.md

IMPORTANT: This is an audit phase. Do NOT modify any source files. Read source files, verify findings, write output only to the FINDINGS-DRAFT.md file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code Quality Audit (AUDIT-01)</name>
  <files>.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md</files>
  <action>
Read the following source files in order to verify the code quality findings from the research. Do not modify them — read only.

Files to read:
- src/components/sound-effects.tsx (oversized component — confirm LOC, structure)
- src/lib/api/admin.ts (large API client — confirm LOC, Promise&lt;any&gt; return types)
- src/app/gameroom/hooks/useGameEvents.ts (handler duplication — verify duplicate ref initialization at lines 40-59 and 152-183)
- src/app/gameroom/page.tsx (inline leaderboard duplication — verify lines 197-233, dead `&& false` at line 211)
- src/app/gameroom/components/PostGameShowcase.tsx (AccoladeChip duplication)
- src/app/gameroom/components/LeaderBoard.tsx (AccoladeChip duplication, filename vs export name mismatch)
- src/app/gameroom/components/SlotTile.tsx (unused interface props — isBonus, revealDelay, entranceDelay)
- src/app/gameroom/hooks/useChatWs.ts (filename vs export name inconsistency)
- src/app/gameroom/components/PostGameModal.tsx (PostgameAccolades export name case mismatch)
- src/app/gameroom/store/gameAtoms.ts (animationStateAtom initial value duplication, botbob string)
- src/app/gameroom/utils.ts (botbob string scatter)
- src/app/gameroom/components/UnifiedMessages.tsx (botbob string scatter)
- package.json (animate.css "latest" unpinned, gsap dependency)

For each file, verify the specific findings from the research. Note if a finding is confirmed, not found, or found with different line numbers than expected.

Write a `## Code Quality` section to `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md` with the following subsections:

### Oversized Components
For each: file path, actual LOC, description of problem, impact rating.

### Duplication
For each: files involved, description of duplicated logic, approximate duplicate lines, impact rating.

### Naming Inconsistencies
For each: file path, inconsistency description (filename vs export vs usage), impact rating.

### Dead Code
For each: file path, line number, what the dead code is, impact rating.

### Complexity Hotspots
For each: file path, function name, description of complexity, impact rating.

Use this format for each individual finding:
```
**[FINDING-QXX]** `path/to/file.ts:LINE` — description
Impact: HIGH/MEDIUM/LOW
Effort: HIGH/MEDIUM/LOW
Remediation: concrete fix description
```

Number findings sequentially: FINDING-Q01, FINDING-Q02, etc.
  </action>
  <verify>
    <automated>test -f /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md &amp;&amp; grep -q "## Code Quality" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md &amp;&amp; grep -q "FINDING-Q" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md &amp;&amp; echo "PASS: Code quality section exists with findings" || echo "FAIL"</automated>
  </verify>
  <done>FINDINGS-DRAFT.md exists with a populated `## Code Quality` section containing at minimum: 2 oversized components, 3 duplication findings, 4 naming inconsistency findings, dead code items, and complexity hotspots — each with Impact/Effort/Remediation fields.</done>
</task>

<task type="auto">
  <name>Task 2: Performance Audit (AUDIT-02)</name>
  <files>.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md</files>
  <action>
Read the following source files to verify the performance findings from the research. Do not modify them — read only.

Files to read:
- src/app/gameroom/hooks/useGameActions.ts (triggerCorrectAnswerEffects — confirm it does NOT read performanceModeAtom, confirm DOM manipulation pattern)
- src/app/gameroom/components/LeaderBoard.tsx (confirm useGameState() call instead of selective atom subscriptions)
- src/app/gameroom/components/AnswerReveal.tsx (confirm useGameState() usage, confirm timeout cleanup gap, confirm type assertion issue)
- src/app/gameroom/components/PostGameShowcase.tsx (confirm useGameState() usage)
- src/app/gameroom/page.tsx (confirm useGameState() for updateGameState when useSetAtom would suffice)
- src/app/gameroom/components/UnifiedMessages.tsx (confirm index key in messages.map)
- src/app/gameroom/components/PostGameModal.tsx (confirm random color generation in getTopAccolades)
- src/components/sound-effects.tsx (confirm no performanceModeAtom check, confirm no dynamic import)
- src/atoms/performance-atom.ts (note the atom's API — what it exports)

Also run these commands to check bundle characteristics:
```bash
cd /Users/kristianoftedal/dev/next-frontend && cat package.json | grep -E '"animate.css|gsap|canvas-confetti"'
```

Append a `## Performance` section to the existing `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md` with the following subsections:

### Re-render Patterns
For each: component name, file path, which atom it subscribes to (full vs derived), better subscription approach, impact rating.

### Animation / Performance Mode Bypass
For each: file path, function/location where bypass occurs, what performanceModeAtom path would look like, impact rating.

### Bundle Characteristics
For each: dependency name, why it's a concern (no lazy loading / unpinned version / possibly unused), impact rating.

### Slow Render Paths
For each: file path, function/effect name, description of problem (uncleaned timers, unstable keys, random on re-render), impact rating.

Use this format:
```
**[FINDING-PXX]** `path/to/file.ts:LINE` — description
Impact: HIGH/MEDIUM/LOW
Effort: HIGH/MEDIUM/LOW
Remediation: concrete fix description
```

Number findings sequentially: FINDING-P01, FINDING-P02, etc.
  </action>
  <verify>
    <automated>grep -q "## Performance" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md &amp;&amp; grep -q "FINDING-P" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md &amp;&amp; echo "PASS: Performance section exists with findings" || echo "FAIL"</automated>
  </verify>
  <done>FINDINGS-DRAFT.md has a populated `## Performance` section containing at minimum: 3 re-render findings (Leaderboard, AnswerReveal, PostGameShowcase), 2 performance mode bypass findings (useGameActions, sound-effects), 2 bundle findings, and 3 slow render path findings — each with Impact/Effort/Remediation.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. File exists: `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md`
2. Contains `## Code Quality` section with FINDING-Q## entries
3. Contains `## Performance` section with FINDING-P## entries
4. Total finding count is at least 15
5. Every finding has Impact, Effort, and Remediation fields
6. No source files were modified (git diff shows no changes to src/)

Run: `git diff --name-only` — should show only `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md`
</verification>

<success_criteria>
- FINDINGS-DRAFT.md exists with both Code Quality and Performance sections
- All major research findings are verified against actual source files (with confirmed line numbers where findings still exist)
- Each finding has Impact (HIGH/MEDIUM/LOW), Effort (HIGH/MEDIUM/LOW), and a concrete Remediation description
- No source files were modified
- Plan 02 can consume this file directly for final consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/05-codebase-audit/05-01-SUMMARY.md` following the standard summary template.
</output>

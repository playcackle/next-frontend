---
phase: 05-codebase-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .planning/phases/05-codebase-audit/FINDINGS.md
autonomous: true
requirements:
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05

must_haves:
  truths:
    - "Architecture findings are documented covering hook boundaries, state management anti-patterns, and data flow"
    - "Type safety findings are documented with exact file:line locations for every any/unknown/@ts-ignore usage"
    - "TypeScript compiler output is captured and recorded"
    - "A single consolidated FINDINGS.md exists with all findings across all four dimensions"
    - "Every finding in FINDINGS.md has Impact and Effort ratings"
    - "FINDINGS.md is sorted so high-impact/low-effort (quick wins) appear first within each section"
    - "Each finding has a concrete remediation recommendation"
  artifacts:
    - path: ".planning/phases/05-codebase-audit/FINDINGS.md"
      provides: "Consolidated prioritized audit report"
      contains: "## Summary\n## Quick Wins\n## Code Quality\n## Performance\n## Architecture\n## Type Safety"
  key_links:
    - from: "src/app/gameroom/page.tsx"
      to: "FINDINGS.md"
      via: "Rules of Hooks violation — hooks after conditional return"
    - from: "src/atoms/performance-atom.ts + src/contexts/performance-context.tsx"
      to: "FINDINGS.md"
      via: "Dual performance mode systems"
    - from: "src/app/gameroom/components/AnswerReveal.tsx"
      to: "FINDINGS.md"
      via: "as unknown as QuizAnswer type mismatch + latent bug"
    - from: ".planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md"
      to: "FINDINGS.md"
      via: "Consolidation of code quality and performance sections"
---

<objective>
Conduct audit passes 3 and 4 (architecture and type safety), then consolidate all findings into the final prioritized FINDINGS.md.

Purpose: Complete the remaining two audit dimensions — architecture concerns (AUDIT-03) and type safety gaps (AUDIT-04) — then merge all findings from Plan 01's draft and these passes into a single, prioritized FINDINGS.md (AUDIT-05) that will drive v1.2 improvements.

Output: `.planning/phases/05-codebase-audit/FINDINGS.md` — the authoritative, consolidated audit report with all findings across four dimensions, rated by impact and effort, sorted by priority.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codebase-audit/05-RESEARCH.md
@.planning/phases/05-codebase-audit/05-01-SUMMARY.md

IMPORTANT: This is an audit phase. Do NOT modify any source files. Read source files and tooling output only. Write output to FINDINGS.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Architecture and Type Safety Audit (AUDIT-03, AUDIT-04)</name>
  <files>.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md</files>
  <action>
Read the following source files to verify architecture and type safety findings. Do not modify them.

**Architecture files to read (AUDIT-03):**
- src/app/gameroom/page.tsx — verify: (1) hooks called after conditional return at line ~105/112, (2) getBaseWsUrl inline helper at lines ~108-110, (3) useGameState() called for updateGameState setter, (4) inline leaderboard vs Leaderboard component
- src/app/gameroom/store/gameAtoms.ts — verify: (1) animationStateAtom initial value duplicated between atom creation and resetGameStateAtom, (2) addUnifiedMessageAtom botbob detection logic
- src/app/gameroom/hooks/useGameState.ts — verify: useGameState spreads full gameState into return value
- src/atoms/performance-atom.ts — check: what localStorage key does it use? What does it export?
- src/contexts/performance-context.tsx — check: what localStorage key does it use? Is it a parallel implementation?
- src/app/gameroom/hooks/useGameEvents.ts — verify: onEvent cleanup return value not used (lines ~185-209), handleLobbySyncRef duplicate definition pattern
- src/app/gameroom/hooks/useGameSocket.ts — check: dual socket architecture, sendEvent debounce location
- src/app/gameroom/components/UnifiedMessages.tsx — verify: botbob detection in component vs gameAtoms.ts

**Type safety files to read (AUDIT-04):**
- src/app/gameroom/utils.ts — verify: @ts-ignore at lines 59 and 61 for window.playSoundEffect
- src/app/gameroom/components/AnswerReveal.tsx — verify: `as unknown as QuizAnswer` at line ~30; confirm QuizAnswer.id type is number and Slot.id is string; check if visibleAnswers.includes(x.id) would always fail
- src/app/gameroom/hooks/useGameEvents.ts — verify: `data: any` in game_over handler at line ~199; sendEventRef cast at line ~77
- src/app/gameroom/hooks/useGameSocket.ts — verify: `data: any` in generic socket.on handler at line ~214; `error?: any` parameter
- src/app/gameroom/hooks/useGameActions.ts — verify: `animationUpdate: any` parameter at line ~72
- src/app/gameroom/hooks/useGameState.ts — verify: `triggerAnimation(type: string, value: any)` signature
- src/app/gameroom/types/payloads.ts — verify: `submit_answer: any` in EventPayloadMap at line ~204
- src/app/api/admin/[...path]/route.ts — verify: `context.params as any` pattern
- src/types/navigator.d.ts — note: Window interface extension opportunity alongside this file
- src/lib/api/admin.ts — spot-check: multiple Promise&lt;any&gt; return types (lines ~658, 674, 691, 710)

Also run these commands and capture the output:
```bash
cd /Users/kristianoftedal/dev/next-frontend && npx tsc --noEmit 2>&1 | head -50
```
```bash
cd /Users/kristianoftedal/dev/next-frontend && npm run lint 2>&1 | head -60
```
```bash
cd /Users/kristianoftedal/dev/next-frontend && grep -rn "@ts-ignore\|as any\|as unknown" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules"
```

Write findings to `.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md` with two sections:

**## Architecture Findings**
Subsections: Hook Boundary Issues, State Management Anti-Patterns, Data Flow Issues.

**## Type Safety Findings**
Subsections: @ts-ignore Usage, `as any` Usage, `as unknown as` Assertions, EventPayloadMap Gaps, Missing Return Types.

**## Tooling Output**
Capture: `tsc --noEmit` output, `npm run lint` output, grep results for any/ts-ignore/unknown.

Use this format for each finding:
```
**[FINDING-AXX]** `path/to/file.ts:LINE` — description
Impact: HIGH/MEDIUM/LOW
Effort: HIGH/MEDIUM/LOW
Remediation: concrete fix description
```
Architecture findings: FINDING-A01, FINDING-A02, ...
Type safety findings: FINDING-T01, FINDING-T02, ...

Note the key latent bug: In AnswerReveal.tsx, `as unknown as QuizAnswer` hides a string/number type mismatch that likely causes `visibleAnswers.includes(x.id)` to always return false (comparing number[] against a string). This is a HIGH impact bug, not just a type gap. Document it as both a type safety finding AND cross-reference it from the bug section.
  </action>
  <verify>
    <automated>test -f /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md &amp;&amp; grep -q "## Architecture Findings" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md &amp;&amp; grep -q "## Type Safety Findings" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>05-02-AUDIT-NOTES.md exists with Architecture Findings (covering hook boundary violation, dual performance systems, state management anti-patterns) and Type Safety Findings (covering @ts-ignore, as any occurrences, the AnswerReveal latent bug, EventPayloadMap gaps). Tooling output from tsc and lint is captured.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate All Findings into FINDINGS.md (AUDIT-05)</name>
  <files>.planning/phases/05-codebase-audit/FINDINGS.md</files>
  <action>
Read `.planning/phases/05-codebase-audit/05-01-FINDINGS-DRAFT.md` (code quality + performance findings from Plan 01) and `.planning/phases/05-codebase-audit/05-02-AUDIT-NOTES.md` (architecture + type safety findings from Task 1).

Write `.planning/phases/05-codebase-audit/FINDINGS.md` as the authoritative consolidated audit report.

**FINDINGS.md structure:**

```markdown
# Codebase Audit Findings
**Date:** 2026-03-12
**Codebase:** Quiz Game Frontend (~13,000 LOC)
**Scope:** src/ — all components, hooks, atoms, utilities

---

## Summary

[3-5 sentence executive summary. State the overall health of the codebase, the most critical findings, and the top recommendation.]

**Finding counts:**
- Total findings: N
- HIGH impact: N (X bugs, Y quality/perf/arch/type issues)
- MEDIUM impact: N
- LOW impact: N

---

## Bugs (Confirmed, Fix Immediately)

[Findings that are confirmed runtime bugs — not just style issues. Currently known: AnswerReveal type mismatch causing animation never to fire; Rules of Hooks violation in page.tsx.]

[Each entry:] **[FINDING-ID]** `file:line` — description. Impact: HIGH. Effort: LOW. Remediation: ...

---

## Quick Wins (HIGH impact, LOW effort)

[Findings where Impact=HIGH and Effort=LOW, or Impact=MEDIUM and Effort=LOW. Sort by impact descending.]

---

## Code Quality (AUDIT-01)

[All FINDING-Q## entries from 05-01-FINDINGS-DRAFT.md, sorted HIGH→MEDIUM→LOW impact.]

---

## Performance (AUDIT-02)

[All FINDING-P## entries from 05-01-FINDINGS-DRAFT.md, sorted HIGH→MEDIUM→LOW impact.]

---

## Architecture (AUDIT-03)

[All FINDING-A## entries from 05-02-AUDIT-NOTES.md, sorted HIGH→MEDIUM→LOW impact.]

---

## Type Safety (AUDIT-04)

[All FINDING-T## entries from 05-02-AUDIT-NOTES.md, sorted HIGH→MEDIUM→LOW impact.]

---

## Priority Order for v1.2

Ordered list of all findings by impact/effort ratio (quick wins first, large refactors last):

| Priority | Finding ID | File | Summary | Impact | Effort |
|----------|-----------|------|---------|--------|--------|
| 1 | FINDING-XXX | ... | ... | HIGH | LOW |
...

---

## Tooling Output

### TypeScript (`npx tsc --noEmit`)
[Paste actual output from Task 1]

### Lint (`npm run lint`)
[Paste actual output from Task 1]
```

Rules for the consolidated report:
1. Every finding from the draft and notes must appear exactly once
2. The "Bugs" section at the top captures findings that are confirmed runtime failures (not just code smell) — currently the AnswerReveal animation-never-fires bug and the Rules of Hooks violation are confirmed bugs
3. "Quick Wins" is a curated highlight section — findings also appear in their dimension section below
4. The priority table is the primary output for v1.2 planning — order matters
5. Every finding entry must include: finding ID, file path with line number, description, Impact, Effort, Remediation
6. Do not assign new finding IDs — use the IDs from the draft and notes (FINDING-Q##, FINDING-P##, FINDING-A##, FINDING-T##)
  </action>
  <verify>
    <automated>test -f /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "## Summary" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "## Quick Wins" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "## Priority Order" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "AUDIT-01\|Code Quality" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "AUDIT-02\|Performance" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "AUDIT-03\|Architecture" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; grep -q "AUDIT-04\|Type Safety" /Users/kristianoftedal/dev/next-frontend/.planning/phases/05-codebase-audit/FINDINGS.md &amp;&amp; echo "PASS: FINDINGS.md exists with all required sections" || echo "FAIL"</automated>
  </verify>
  <done>FINDINGS.md exists with: executive summary, confirmed bugs section, quick wins section, all four dimension sections (Code Quality, Performance, Architecture, Type Safety), and a priority order table. Every finding has Impact, Effort, and Remediation. No source files were modified.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `FINDINGS.md` exists in `.planning/phases/05-codebase-audit/`
2. Contains sections for all five requirements: Code Quality (AUDIT-01), Performance (AUDIT-02), Architecture (AUDIT-03), Type Safety (AUDIT-04), Priority Table (AUDIT-05)
3. The AnswerReveal latent bug (string/number id mismatch) is documented in the Bugs section
4. The Rules of Hooks violation in page.tsx is documented
5. The dual performance mode systems finding is documented
6. Every finding has Impact, Effort, and Remediation fields
7. No source files were modified: `git diff --name-only` shows only files under `.planning/`
</verification>

<success_criteria>
- FINDINGS.md is the single authoritative output for the entire Phase 5 audit
- All five requirements (AUDIT-01 through AUDIT-05) are satisfied by the content
- The priority table gives v1.2 planners a clear, ordered list of what to fix first
- Confirmed runtime bugs are surfaced at the top (not buried in dimension sections)
- Tooling output (tsc, lint) is captured for reference
- Zero source files modified throughout the entire audit
</success_criteria>

<output>
After completion, create `.planning/phases/05-codebase-audit/05-02-SUMMARY.md` following the standard summary template.
</output>
